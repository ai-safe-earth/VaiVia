# Redesign & Delivery Plan

Living roadmap for VaiVia. Ratified 2026-08-15 after an architecture review. Update this file as phases land.

---

## Context

The original skeleton had a sound core (then framed as a two-source OSM + Trailforks knowledge graph) but incomplete or wrong surroundings:

- The LLM layer — the product's point — was undesigned and implied raw LLM-generated Cypher (injection + hallucination risk).

(The data story changed on 2026-08-18: Trailforks is legally unavailable — API-only key grants, prior written consent required for commercial/in-software/AI use, see `docs/licensing.md` — so the graph is OSM throughout, with open-licensed Wikipedia/Wikidata enrichment over marquee places. Trailforks references below this point are historical record of what was built and verified at the time.)
- The routing model was internally inconsistent: `CONNECTS_TO` was Segment→Intersection, but GDS routing requires Intersection–Intersection edges.
- `COMPOSED_OF`/`MAPS_TO` were redundant and unordered, silently breaking distance-along-trail queries ("hut at the halfway point").
- No security, auth, observability, or deployment story.

## Product decisions

| Decision | Choice |
|---|---|
| Ambition | Public beta product (real users, abuse protection, LLM cost caps) |
| UX | Chat-first. One UI at every width: the conversation is the base layer, the composer never leaves the bottom, the map is an overlay a route tap brings up (Phase 11) |
| Frontend | Next.js (independent app) |
| Gateway | Fastify (Node/TS) — auth, rate limits, origin control, SSE proxy; **no business logic** |
| Backend | FastAPI (Python) — chat orchestration + graph query service |
| Graph DB | Neo4j 5 **Community** + APOC + GDS |
| Auth + relational store | Supabase (JWT auth; Postgres for chat history, quotas, cost ledger) |
| LLM | OpenAI (structured-output intent extraction; text-embedding-3-small) |
| Hosting | Single VPS + docker-compose now; 12-factor so pieces can move to PaaS later — see Deploy strategy |
| Environments | **One: production**, a single VPS deployed from `main`. `develop` has no server: the local stack (`infra/docker-compose.yml` + `cd infra && supabase start`) is the development environment and CI is its gate. No staging until a second person deploys or a release breaks production twice. |
| Branches | `main` = production (protected; release PRs and `hotfix/*` only). `develop` = repository default; every PR targets it. `feat/ fix/ docs/ chore/` off `develop`; `hotfix/*` off `main`, merged back into `develop` in the same sitting (`CONTRIBUTING.md:303-344`). |
| Release | PR `develop → main`, CI green, merge. The merge deploys: Actions builds the three images (`:<sha>`), pushes to GHCR, SSHes to the VPS, runs `deploy.sh <sha>` (migrate → pull → up → healthz). Tag the merge commit `vX.Y.Z` by hand once healthz passes. Rollback = `deploy.sh <previous sha>`. |
| Repo | This monorepo: `frontend/`, `gateway/`, `backend/`, `infra/`, `docs/` |
| Beta data scope | Lecco + Bergamo bboxes (`REGIONS`); catalogue generated over both |
| Streaming | SSE end-to-end from day one |

## Target architecture

```
Browser (Next.js app)
   │  HTTPS + Supabase JWT
   ▼
Caddy (TLS) ── Fastify GATEWAY  (public: the ONLY exposed service)
   │   • Supabase JWT validation (JWKS), origin/CORS control
   │   • per-user + per-IP rate limits, daily LLM quota check (Postgres)
   │   • request IDs, structured logs, SSE passthrough
   ▼  internal docker network only
FastAPI BACKEND (chat orchestration + graph query service)
   │   • /chat: OpenAI structured-output intent extraction (NEVER raw Cypher)
   │   • intent JSON validated (pydantic) → parameterized query template
   │   • conversation history + cost ledger in Supabase Postgres
   ▼
Neo4j Community + APOC + GDS (internal only)
   ▲
Ingestion jobs (Overpass w/ backoff+cache, Trailforks --mock, spatial matcher)
```

Security model: browsers never reach FastAPI, Neo4j, or OpenAI. The gateway is the single ingress; the backend trusts only the gateway (shared-secret header + network isolation); LLM calls happen server-side with per-user cost caps enforced before each call.

## Graph model (corrected)

1. **Routing graph:** `(:Intersection)-[:CONNECTS_TO {distance, elevation_change, osm_way_id, surface, highway_type}]->(:Intersection)`. Segments are edge data on the routing graph; `(:Segment)` nodes remain for trail composition and POI proximity. GDS Dijkstra projects Intersection/CONNECTS_TO consistently.
2. **`[:MAPS_TO]` is dropped.** Single ordered relationship `(:Trail)-[:COMPOSED_OF {seq, match_confidence}]->(:Segment)`; `seq` makes distance-along-trail queries correct.
3. **Region:** numeric bbox properties; `(:Trail)-[:LOCATED_IN]->(:Region)` and `(:POI)-[:LOCATED_IN]->(:Region)` created at ingestion time.
4. Routing queries never traverse semantic edges (`PASSES_BY`); all traversals bounded.

## The LLM boundary (intent contract)

`backend/chat/intents.py` defines pydantic models — `TrailSearchIntent`, `LoopSearchIntent`, `RouteIntent`, `SemanticThemeIntent`, and `ClarifyIntent`, wrapped in a `PlanEnvelope` of atomic subqueries. OpenAI structured outputs (strict json_schema, via `to_strict_schema` because strict mode rejects `oneOf`/`discriminator`) produce validated intents only; out-of-scope input → `ClarifyIntent`, which poisons the whole plan. `backend/chat/composer.py` — Python, not the model — merges subqueries (tightest-wins) and maps the result onto named parameterized templates in `backend/graph/queries.cypher`. **The model never sees or writes Cypher.**

## Deploy strategy

Ratified 2026-08-28. One VPS, one compose project at `/srv/vaivia/`, Caddy in front, images from GHCR, one script for deploy, one for backup, one for catalogue publish. Nothing below exists yet: the repo has no Dockerfile, Caddyfile, `*.sh`, `.dockerignore`, `uv.lock`, prod compose or deploy job (`infra/docker-compose.yml:152-153` still says "added when their Dockerfiles land"). Decisions live here; commands live in `docs/deploy.md` (D2).

### Topology

```
internet ──443──▶ caddy  (auto-TLS; the only published ports on the box: 80/443)
                   ├─ /api/*  → gateway:3001   handle_path strips /api; flush_interval -1 so /chat SSE is never buffered
                   └─ /*      → frontend:3000
                   compose network `internal`:  gateway → backend:8000 → neo4j:7687
neo4j bolt stays on 127.0.0.1:7687 of the VPS (the dev binding, docker-compose.yml:18): the owner's
ssh -L for catalogue loads and ingestion; never a public port
```

| Decision | Choice | Why |
|---|---|---|
| Services on the VPS | caddy, frontend, gateway, backend, neo4j | `graphhopper`: no code calls it (routing is comfort-weighted GDS Dijkstra; the `docs/routing-engine.md` migration never started). `postgis`: the pipeline's working store; `export.neo4j_load` writes its provenance row into it (`pipeline/export/neo4j_load.py:393-409`), so loads run from the workstation, never the server |
| Ingress *(recommended)* | Caddy, one domain, gateway under `/api` | same origin: no CORS preflight on the SSE POST, one cert. `NEXT_PUBLIC_GATEWAY_URL=https://<domain>/api` works because `frontend/lib/api.ts:40` concatenates paths and `frontend/app/api/` does not exist. `ALLOWED_ORIGINS=https://<domain>` (`gateway/src/config.ts:59`) |
| Gateway reachability | compose network only | `trustProxy: true` (`gateway/src/app.ts:46`) is safe only while nothing but Caddy can set `X-Forwarded-For`; one replica, since `@fastify/rate-limit` is in-process (`app.ts:77-92`) |
| Compose files | `infra/docker-compose.yml` (dev) + `infra/compose.prod.yml` overlay | overlay adds caddy + the three app services, pins `neo4j.image`, and sets every compose-network address in each service's `environment:` — `NEO4J_URI=bolt://neo4j:7687`, `BACKEND_URL=http://backend:8000`, `GATEWAY_HOST=0.0.0.0`, `ROUTE_DOCUMENTS_DIR=/documents/current`, `NODE_ENV=production` — because compose `environment` beats `env_file`, so a `.env` copied from `.env.example` cannot undo them and `config.ts:40-41` cannot be flipped off production. `graphhopper` and `postgis` get `profiles: ["dev"]` in the dev file: `up -d postgis` still starts it locally and prod `up -d` skips both; the header at `docker-compose.yml:1-3` changes with it. One neo4j definition, so the allowlist (`:37`, `:45`), transaction timeout (`:51`), memory (`:54-56`) and plugins volume (`:84`) cannot drift |
| Neo4j image *(recommended)* | pin `neo4j:5.x.y-community` in prod (dev floats on `5-community`, `:11`) | the GDS jar on `neo4j_plugins` is seeded by hand (`CONTRIBUTING.md:72-96`, 2.13.12 at `:89`) and losing it is silent — routing falls back to hop-count `shortestPath` (`CONTRIBUTING.md:68-70`); a minor bump would strand it |
| Images *(recommended: public)* | `ghcr.io/ai-safe-earth/vaivia-{backend,gateway,frontend}:<sha>` (+ `:main`) | built by Actions on push to `main`; the repo is public, so public packages and no pull token on the VPS |
| Process manager | compose `restart: unless-stopped` + healthchecks that match the **body** | backend `/healthz` answers 200 with `"database":"down"` when Neo4j is unreachable (`api/main.py:116-124`, `tests/test_api_middleware.py:67-70`), so probes match `"database":"up"`, never the status |
| VPS *(recommended)* | 4 vCPU / 8 GB / 80 GB, EU, provider daily snapshot on | neo4j alone is heap 2g + pagecache 1g (`docker-compose.yml:54-56`); the Supabase pooler is `eu-west-1` (`.env.example:73`) |

### Artefacts

All three images build from the **repo root** context with one root `.dockerignore` (`.env*`, `**/.venv`, `**/node_modules`, `**/.next`, `review/`, `infra/neo4j/plugins/`) — `.gitignore` guards git, not a build context, and a laptop `.env` baked into a public image is a leak.

| File | What |
|---|---|
| `backend/Dockerfile` | `python:3.11-slim` + uv (`backend/pyproject.toml:7`); `uv sync --frozen --no-dev` (commit `backend/uv.lock`); CMD `uvicorn api.main:app --host 0.0.0.0 --port 8000` (`api/main.py:3`). Scripts ship in the image: `docker compose exec backend python -m scripts.<x>` is the server-side console |
| `gateway/Dockerfile` | `node:22-alpine` (CI pins 22, `ci.yml:48`); `npm ci && npm run build && npm ci --omit=dev`; CMD `node dist/server.js` (`gateway/package.json:12-13`). `NODE_ENV=production` comes from the overlay, so `config.ts:47-52` refuses `GATEWAY_DEV_NO_AUTH` and `:64-68` refuses a missing secret/JWKS URL |
| `frontend/Dockerfile` | `output: 'standalone'` added to `next.config.mjs`; build ARGs `NEXT_PUBLIC_GATEWAY_URL`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` — inlined at build (`next.config.mjs:6-8`, `lib/supabaseClient.ts:23-24`), so they are Actions **vars**, not runtime env; CMD `node server.js` on 3000 |
| `infra/compose.prod.yml` | the overlay: `env_file: /srv/vaivia/.env`; the `environment:` block above; backend mounts `/srv/vaivia/documents:/documents:ro` and `/srv/vaivia/migrations` at the path `apply_migrations.py:30` resolves (`parents[2]/infra/supabase/migrations`); json-file logging `max-size 10m, max-file 5`; healthchecks |
| `infra/Caddyfile` | the two routes above, `{$DOMAIN}` from env |
| `infra/deploy.sh` | `deploy.sh <sha>`: **preflight** (every required key non-empty in `/srv/vaivia/.env`: `GATEWAY_SHARED_SECRET NEO4J_PASSWORD OPENAI_API_KEY DATABASE_URL SUPABASE_URL SUPABASE_JWT_JWKS_URL ALLOWED_ORIGINS DOMAIN`; `GATEWAY_DEV_NO_AUTH`, `NODE_ENV`, `NEO4J_URI`, `BACKEND_URL` must be absent) → **migrate** (`docker compose exec backend python -m scripts.apply_migrations` on the running container over the mounted SQL the job just copied; idempotent, one transaction per file, `DATABASE_URL` from the env_file — the image holds no `.env` — `apply_migrations.py:7`, `:33-44`, `:77-78`; "No .sql files" exits 1, `:59-61`, fatal) → write `IMAGE_TAG=<sha>` → **pull** → **up** `-d --remove-orphans`, wait healthy → **healthz** (`GET https://$DOMAIN/api/healthz` 200, `GET https://$DOMAIN/` 200, `cypher-shell 'RETURN gds.version()'` answers). Non-zero on any failure; nothing rolls back on its own. The first deploy has no running backend: migrations are applied from the workstation as in Phase 0 (its `apply_migrations` bullet), part of the bootstrap in `docs/deploy.md` *(recommended order)* |
| `infra/backup.sh` | nightly cron: `pg_dump` through the pooler into `/srv/vaivia/backups/`, keep 14 |
| `infra/publish-catalogue.sh` | workstation-side, documents → server (below) |
| `docs/deploy.md` | the runbook: VPS bootstrap (once), deploy, rollback, restore, catalogue publish, first fill |

### Secrets

Rule: **CI holds no application secret.**

| Where | What |
|---|---|
| `/srv/vaivia/.env`, `chmod 600`, owner `deploy` | every runtime secret: `OPENAI_API_KEY`, `DATABASE_URL` (Supavisor pooler, session mode — the direct host is IPv6-only, `.env.example:71-73`; TLS by host in code, `backend/core/pg.py:22-43`, `gateway/src/quotaStore.ts:26`), `GATEWAY_SHARED_SECRET` (`openssl rand -hex 32`), a production-only `NEO4J_PASSWORD`, `SUPABASE_URL` + `SUPABASE_JWT_JWKS_URL` (hosted project, `.env.example:46-48`; the gateway pins `iss`/`aud` from the URL, `app.ts:62-63`), `ALLOWED_ORIGINS`, `DOMAIN`, `IMAGE_TAG`. Not `SUPABASE_SERVICE_ROLE_KEY`: nothing in `backend/`, `gateway/` or `frontend/` reads it (`backend/core/config.py:8-75` has no such field), so it never goes on the box. Written by hand over SSH from the password manager; never in CI, never in an image |
| Actions secrets | `DEPLOY_HOST`, `DEPLOY_SSH_KEY` (user `deploy`: docker group, no sudo, key-only), `DEPLOY_KNOWN_HOSTS` |
| Actions vars (public) | the three `NEXT_PUBLIC_*` (`.env.example:79-84`), `DOMAIN` |
| GHCR | `GITHUB_TOKEN` with `permissions: packages: write` |

Known gap: an empty `gateway_shared_secret` silently disables the backend's gateway check (`core/config.py:57-60`, `api/middleware.py:67-68`) — the preflight is the only guard until the boot refusal lands (D1, *recommended*). Go-live state: Supabase auth is parked locally as `dev-local-user` (`GATEWAY_DEV_NO_AUTH=true`); on the VPS the variable is absent and the JWKS URL is the hosted project's, which already verified a real sign-in end to end (Phase 0's auth bullet). The exposed OpenAI key and Supabase passwords rotate before anything lands on a server (D0; `CONTRIBUTING.md:349-351`).

### How data reaches the server

PostGIS stays on the workstation; the VPS receives **documents and a Neo4j load**. Every Neo4j writer runs from the workstation over `ssh -N -L 7688:127.0.0.1:7687 deploy@host` with `NEO4J_URI=bolt://127.0.0.1:7688` — env beats `.env` (`pipeline/core.py:42-44`, `neo4j_load.py:295-297`, `backend/core/config.py:9-13`).

The catalogue is two artefacts that must agree — documents on disk and the `:Route` rows that name them — and the API refuses a disagreement with a 503 (`api/routes/routing.py:209-215`, `:242-246`), so the order is the rule. `infra/publish-catalogue.sh` (`--dry-run` passes through to the loader): **rsync** `review/routes/vv2-*.json` (`review/` is gitignored, `.gitignore:78`; the glob is the loader's, `neo4j_load.py:246`; the API opens `<dir>/<route_id>.json`, `routing.py:238`, so flat) to `/srv/vaivia/documents/<publish-ts>/` (named by publish timestamp, not by a document's `run_id`: after Phase 10 the catalogue is a union of per-recipe, per-region runs, each with its own `provenance.run_id`, the field the loader stamps as `doc_run_id`, `neo4j_load.py:135`; the script records the set of run ids it shipped in `<dir>/RUNS`) → **load** `cd pipeline && uv run python -m export.neo4j_load` through the tunnel (wipes and replaces `:Route/:Place/:Start`, `:309-330`; refuses a count mismatch, `:338-342`) → **flip** `ln -sfn <publish-ts> /srv/vaivia/documents/current` (atomic; the parse cache is keyed on path + mtime + size, `routing.py:259-260`, so no restart; between load and flip re-emitted routes answer `build_mismatch` 503 — seconds, never a wrong shape) → **audit** `docker compose exec backend python -m scripts.audit_catalogue_documents`, exit 0 or the publish is not done (`audit_catalogue_documents.py:25`). Keep the last two run dirs; prune older by hand. Unpublished mapped documents ship harmlessly: the loader gates on kind (`:264-266`) and the API 404s any id the graph does not name (`:227-229`).

First fill (once, over the same tunnel, in `docs/deploy.md`): GDS jar into `neo4j_plugins` (`CONTRIBUTING.md:94-95`); `scripts.init_schema`; `ingestion.osm_ingest --region Lecco` and `--region Bergamo` (`CONTRIBUTING.md:160-171`); `scripts.embed_trails` only if `MATCH (t:Trail) RETURN count(t)` > 0 (a no-op otherwise); then `publish-catalogue.sh`. **Never `trailforks_ingest --mock` against production** (owner decision 13; the `:Trail` question is Phase 8 E5): the fixture is synthetic and it is the only `:Trail` writer, so production has no `:Trail` nodes and `search_trails` answers nothing.

### Backups, restore, rollback

| Store | Irreplaceable | Backup | Restore / rollback |
|---|---|---|---|
| Supabase Postgres `public` — conversations, ledger, quotas, favorites, feedback (0001–0006) | **yes** | `backup.sh` nightly `pg_dump` of `public` as the `postgres` role; the provider's disk snapshot is the off-box copy | `pg_restore` into the local `supabase start` stack, `scripts.smoke_supabase` green — rehearsed once (D5) |
| `auth.users` | yes | **not in our dump** — Supabase's; `public` rows reference it (`0001:7`), so a restore into a fresh project needs auth first, and on the free tier a lost project means users re-register | *(recommended)* free + dump until users justify Pro |
| Neo4j | no — OSM re-ingest + catalogue reload | none; a publish touches only `:Route/:Place/:Start` (`neo4j_load.py:309-330`), the OSM graph is untouched | bad publish: `rsync` the previous run dir back to `review/routes/`, re-run the loader through the tunnel, flip `current` back |
| Documents | no — PostGIS regenerates them | the last two publish dirs under `/srv/vaivia/documents/` | flip `current` |
| `/srv/vaivia/.env`, GDS jar | small, hand-made | password manager; `infra/neo4j/plugins/` local pin (`.gitignore:87`) | rewrite / `docker cp` |
| PostGIS (the source) | **yes** | not on the VPS — the workstation's backup, outside this section | — |

App rollback: `deploy.sh <previous sha>` — images are immutable by sha and migrations are **additive and idempotent** (`apply_migrations.py:7`; `0001:5` `create table if not exists`, `0005:7` `add column if not exists`), so the previous image runs against the newer schema. A drop or rename ships one release after its last reader is gone. `apply_migrations` replays every file every run and keeps no ledger (`:73-79`); a ledger table is the upgrade if a file ever stops being idempotent. Secrets: rotation is forward-only.

### Monitoring

| Signal | Mechanism |
|---|---|
| Up? | one cron on the VPS every 5 min: `curl -fsS https://$DOMAIN/api/healthz` (DNS, Caddy, TLS, gateway) **and** `docker inspect` health of every service → ping a healthchecks.io check; no ping → e-mail. A dead VPS, Caddy, gateway or Neo4j all stop the heartbeat; the backend stays dark (the security model above) |
| Crashed process | compose healthchecks + `restart: unless-stopped` |
| What happened | `docker compose logs <svc> --since … \| grep <request-id>` — backend logs are JSON with request ids (`backend/core/logging.py:1-5`), gateway is pino, the id is echoed to the browser (`app.ts:100-103`) |
| Deploy failed | the `deploy` job is red; the previous containers still serve unless `up` itself failed — then `deploy.sh <previous sha>` |

### Deliberately not built

| Not built | Instead | Build it when |
|---|---|---|
| Staging | CI + the local stack + `npm run test:e2e` | a second person deploys, or a release breaks production twice |
| Swarm / k8s / blue-green | compose; seconds of restart | not at this scale |
| Log shipping, dashboards (the Phase 6 deploy bullet) | `docker compose logs` | a user problem the logs cannot answer by hand |
| Auto-rollback | red job → `deploy.sh <sha>` by hand | it has been needed three times |
| Secrets manager, Terraform | `.env` chmod 600; one VPS by hand, steps in `docs/deploy.md` | a second operator / a second VPS |
| Neo4j dumps | the loader re-run + the rebuild path | never on Community (dump needs the store stopped) |
| Image builds on every PR | build on push to `main` only; D2 proves the Dockerfiles locally | a broken Dockerfile reaches `main` twice |
| GraphHopper container | GDS Dijkstra | the `docs/routing-engine.md` migration lands (JVM, ~1 GB heap, `:102-103`) |

## Release process

### What runs when

| Event | Runs |
|---|---|
| any PR, push to `develop` | the four CI jobs (`ci.yml:9-71`). No server |
| push to `main` | CI → `images` pushes `:<sha>` and `:main` to GHCR → `deploy` (`needs:` all five; `concurrency: deploy-production`, never cancelled): `scp` `infra/{compose.prod.yml,Caddyfile,deploy.sh,backup.sh}` and `infra/supabase/migrations/` to `/srv/vaivia/`, then `ssh deploy@host /srv/vaivia/deploy.sh <sha>` |
| tag `v*` | nothing; a tag names what is already live |
| catalogue change | `publish-catalogue.sh` by hand from the workstation — not a release; note the publish timestamp and its `RUNS` in `handoff.md` |
| cron on the VPS | `backup.sh` nightly; heartbeat every 5 min |

### Release checklist (PR `develop → main`)

1. `develop` green; `handoff.md` says what ships; the migrations the release needs are in the PR body (0006 is current; `deploy.sh` migrates before `up`, so "before the backend" is automatic — and 0006 widens a primary key the running backend still writes against, so an old image left up during the window gets 42P10 on `/feedback` until it is replaced).
2. `cd frontend && npm run test:e2e` against the local stack (`playwright.config.ts:1-8`: pre-deploy, never CI). The Phase 8 regression policy (README): every release PR carries `eval_golden --graph --answers` (+ `--judge` after E4); `scripts.check_intents_live` 7/7 clarify when a prompt or intent changed.
3. Graph or documents changed → publish the catalogue first: old code reads new documents through the `schema_version` gate (`routing.py:196-203`); new code reading old documents may not.
4. New migration → additive + idempotent. New env var → in `/srv/vaivia/.env` **before** merging, and in the `deploy.sh` preflight in the same PR.
5. PR titled `release: vX.Y.Z`; merge — no rebase, no cherry-pick (`CONTRIBUTING.md:333-335`).
6. `deploy` green → `git tag -a vX.Y.Z <merge sha> && git push origin vX.Y.Z`. Red → read the job; `deploy.sh <previous sha>` if production is affected; fix forward on `develop`.
7. `E2E_BASE_URL=https://<domain> E2E_LIVE=1 npm run test:e2e` (`playwright.config.ts:20`, `e2e/smoke.spec.ts:30`; a dedicated production test account; one OpenAI turn). `handoff.md` gets the sha in production.

### Hotfix

`git switch -c hotfix/<x> main` → PR to `main` → merge (deploys) → `git switch develop && git merge main` in the same sitting (`CONTRIBUTING.md:336-338`). Tag with a patch bump.

## Delivery phases

### Phase 0 — Restructure & foundations ✅
- [x] This plan committed as `docs/plan.md`; architecture docs corrected.
- [x] Monorepo layout: `backend/`, `gateway/`, `frontend/`, `infra/`.
- [x] `infra/docker-compose.yml`: Neo4j Community, localhost-only port binding for dev.
- [x] CI (GitHub Actions): backend lint/format/test, offline.
- [x] Supabase Postgres migration drafts (`infra/supabase/migrations/`).
- [x] Supabase project created; `0001_chat_and_quotas.sql` applied (four tables, RLS on, one policy each). Applied with `uv run python -m scripts.apply_migrations` rather than the Supabase CLI, which expects its own `supabase/migrations` layout. The direct host `db.<ref>.supabase.co` is IPv6-only, so `DATABASE_URL` points at the Supavisor pooler in session mode (port 5432).
- [x] Email auth configured (email provider only, signups on, autoconfirm off) and a real sign-in exercised end to end: password grant returns an ES256 token whose `kid` matches the project JWKS, and the running gateway verifies it against the live JWKS — no token 401, malformed token 401, real token passes auth and the quota pre-check and is only stopped by the backend being down.

### Phase 1 — Graph core & ingestion (the moat) ✅
Ontology extended and owner-validated before build: difficulty trio (label +
level 1–4 + notes), per-activity durations (DIN 33466 hike / speed-by-level
MTB), elevation gain/loss at trail, segment, and per-direction edge level,
seasonality lists (`best_seasons`, `seasonal_hazards`), `landscape_description`
feeding the Phase 3 embedding alongside description and difficulty notes.
- [x] `backend/graph/schema.cypher` (corrected model) + `backend/scripts/init_schema.py` (applies schema, seeds region from `DEFAULT_BBOX`).
- [x] `backend/ingestion/osm_ingest.py`: Overpass with backoff + on-disk cache (`overpass_client.py`); pure topology extractor (`osm_extract.py`) splits ways at intersections into deterministic `"<wayId>#<n>"` pieces and builds the Intersection/CONNECTS_TO routing graph (oneway-aware, both directions); MERGE-idempotent loaders.
- [x] `backend/ingestion/trailforks_ingest.py --mock` + `fixtures/trailforks_mock.json` (full ontology); `spatial_match.py` creates ordered `COMPOSED_OF {seq, match_confidence}` with activity/highway compatibility checks (delete-and-recreate so re-runs never leave stale links).
- [x] Tests (29, offline): topology split/oneway/determinism, matcher precision incl. the 15 m parallel-trail residual risk, DIN/MTB duration formulas, fixture normalization.
- [x] Live-DB smoke: `uv run python -m scripts.smoke_graph` runs both ingesters twice against the compose Neo4j and compares counts. Passes — 15,937 Segment, 15,451 Intersection, 31,848 CONNECTS_TO, 489 PASSES_BY, identical across both passes, so the MERGE keys are stable. Fixed on the way: the schema loader split `.cypher` files on `;` before stripping comments, so a semicolon inside a comment sent `durations are MINUTES.` to the server as Cypher, and Overpass rejected the default httpx User-Agent with 406 so OSM ingestion could never run.
- [x] Fixture geometry re-cut along real OSM ways (`scripts/make_trailforks_fixture.py` traces connected segment chains from the ingested graph, per-activity highway types, all metadata preserved). Spatial matching now produces 15/9/15 `COMPOSED_OF` edges for the three trails, idempotent across re-ingestion (39 edges, stable). The original synthetic geometry sat ~106 m from the nearest real way against the 20 m threshold, so `COMPOSED_OF` had never existed.

### Phase 2 — Backend query service ✅
- [x] FastAPI app (`backend/api/`): `POST /trails/search`, `GET /trails/{id}`, `GET /trails/{id}/geojson`, `POST /routes`, `GET /healthz`.
- [x] Named-template library `backend/graph/queries.cypher` + `query_loader.py`; `Neo4jClient.run_named()` runs templates by name with parameters only. `TrailSearchRequest` is shaped to match Phase 4's `TrailSearchIntent` 1:1.
- [x] Routing: POI resolution → nearest-intersection snap (point index, bounded radius) → bounded `shortestPath` over `CONNECTS_TO` only; distance capped by settings.
- [x] Gateway-trust middleware (`X-Gateway-Secret`, `/healthz` public) + request-id propagation + structured JSON logging.
- [x] 34 API/template tests (63 backend total), no Neo4j needed — fake graph client records template name + parameters. Guard tests assert no template mutates data, none traverses semantic edges in a path, and no traversal is unbounded.
- [x] GDS Dijkstra wired and verified live. `scripts/smoke_routing.py` proves the templates against the real instance — on a 2.3 km test pair, Dijkstra (metre-weighted) found 2322 m where hop-count shortestPath found 2474 m. `/routes` now prefers GDS over a per-request bbox projection (unique name, always dropped in `finally`), enriches the node-only GDS stream via the new `route_edge_details` template (gain, surfaces, way ids), and falls back to shortestPath when GDS is unavailable — a real failure mode: the plugin silently skips installation when its network fetch fails at container start. Verified end to end over HTTP: a live 223 m POI-to-POI route served by GDS with real surface data, and a correct 404 for a disconnected pair.

### Phase 3 — Gateway (security layer) ✅
- [x] Fastify 5 + TypeScript (`gateway/`): Supabase JWT verification via remote JWKS (`jose`), `@fastify/rate-limit` keyed by verified user id with IP fallback, strict CORS allowlist, `@fastify/http-proxy` (SSE-capable) for `/trails`, `/routes`, `/chat` only — every other path 404s at the gateway.
- [x] Request pipeline ordered so identification runs *before* rate limiting (limits key on the verified user; unauthenticated traffic is still IP-counted rather than escaping on an early 401), with enforcement at the route preHandler.
- [x] LLM quota pre-check on `/chat` against Supabase `daily_quotas`; fails open on Postgres errors (logged) so a database blip degrades cost control, not availability.
- [x] Proxy attaches `X-Gateway-Secret`, `X-Request-ID`, and verified `x-user-id`; the caller's bearer token is never forwarded.
- [x] 25 tests (real RSA-signed JWTs, stub upstream, SSE stream assertion): 401 paths, unknown-key and expired tokens, per-user limit isolation, 429 on limit and on exhausted quota with the backend never called, CORS allow/deny, request-id adoption, health.
- [x] CI job (npm ci → lint → typecheck → test); clean `tsc --noEmit` and eslint.

### Phase 4 — Chat orchestration (LLM) ✅
- [x] `POST /chat` streams SSE (`conversation`, `intent`, `results`, `token`…, `done`, `error`). Identity comes from the gateway's `X-User-Id`; the backend never parses JWTs.
- [x] Intent contract (`backend/chat/intents.py`): `TrailSearchIntent | RouteIntent | ClarifyIntent`, pydantic-validated and dispatched to named templates **in Python**. The model never sees Cypher, never names a template, never supplies an identifier. Adversarial or out-of-scope input → `Clarify`, which runs no query at all.
- [x] OpenAI strict structured outputs. Pydantic's tagged-union schema needed transforming: strict mode rejects `oneOf` and `discriminator`, so `to_strict_schema()` rewrites to `anyOf`, closes every object, and marks every property required.
- [x] Grounded answers: the answer model only sees results the graph returned; `result_refs` pins every reply to real trail ids.
- [x] Quota enforced before any model call (authoritative; the gateway also pre-checks). Usage written to the ledger after each turn.
- [x] History persisted per conversation with an ownership check — one user cannot continue another's conversation.
- [x] 33 offline tests (96 backend total): pipeline, dispatch, quota, history, ownership, injection containment.
- [x] **Live verification** — `uv run python -m scripts.check_intents_live`: 15/15 against the real API (8 natural phrasings → correct intents and fields; 7 injection/out-of-scope payloads → all `clarify`). Costs money, so it is a script, not CI.
- [x] Swap `InMemoryStore` for the written `PostgresStore` (asyncpg). The lifespan now opens a pool when `DATABASE_URL` is set and falls back to in-memory only when it is not — previously it warned about the missing variable and then used `InMemoryStore` either way. Verified against the live database by `uv run python -m scripts.smoke_supabase` (12 checks inside a transaction that is always rolled back).

### Phase 5 — Frontend ✅
- [x] Next.js 15 + React 19 app (`frontend/`): chat-first two-pane layout (superseded by Phase 11), streaming replies, trail result cards, MapLibre map drawing the selected trail's real geometry.
- [x] `lib/sse.ts` — incremental SSE parser that survives frames split across arbitrary network chunks (mid-JSON, between `event:` and `data:`, on the separator itself); 13 tests including a character-by-character stream.
- [x] `lib/api.ts` — the app's only network surface: gateway URL from env, Supabase bearer attached, 401/429 mapped to usable messages, malformed frames skipped rather than tearing down the stream.
- [x] `lib/format.ts` — the single place metres/minutes become human units.
- [x] Map: OSM raster tiles (no API key, correct attribution), auto-fit bounds; `dynamic(ssr: false)` since MapLibre touches `window` at import. The tiles are desaturated and darkened at the raster layer (not by a CSS filter, which would take the route line with it) so the light basemap sits on the dark ground; the route line is lime at 3px with no casing. The 2026-08-27 difficulty band ramp (lime/amber/flare/muted, mtb near-black) was withdrawn 2026-08-28: every line is lime, selection is width/opacity only (Phase 11 U0).
- [x] Verified: 25 tests pass, `next build` compiles and type-checks clean, production server serves the rendered app, and no secret appears in the bundle.
- [x] Sign-in page + conversation list. Email/password auth (`AuthPanel`, sign-up with the confirm-email flow, friendly error mapping), a session bar with sign-out, and a conversation list read straight from Supabase under the migration's RLS policies — select-only, `auth.uid() = user_id`, which is what those policies were written for; writes still go only through the backend. Selecting a conversation loads its history and resumes it (the page remounts `ChatPanel` via `key`, so no state leaks across switches). Verified in a real browser against the full live stack: sign-in as a real user, history resumed, a live streamed follow-up turn answered with a graph-grounded trail, and its geometry drawn on the map. RLS also verified negatively: the anon role sees zero rows.
- [x] Playwright end-to-end smoke (`frontend/e2e/smoke.spec.ts`, `npm run test:e2e`). Four tests against the full running stack: wrong password gets a human sentence, sign-in/sign-out round-trip, resuming a stored conversation renders its history, and — behind `E2E_LIVE=1`, since it costs an OpenAI turn — a live streamed answer whose trail card draws geometry on the map. Credentials come from `E2E_EMAIL`/`E2E_PASSWORD`, never the repo; the suite skips cleanly when they are unset, so CI stays offline. First run caught a real bug: creating a conversation remounted `ChatPanel` mid-stream (the panel key tracked `selected`) and destroyed the streaming answer — the key now changes only on explicit navigation. 4/4 passing.
- [x] Brand system v1.0 applied (`assets/brand/BRAND-SPEC.md`): dark ground, two accents (lime = route/action/confirmed, flare = hazard/stale/missing), square corners, 1px hairlines, no shadows. Tokens are imported once globally (`app/tokens.css`) and no component holds a hex literal — the single exception is MapView, which resolves `--vv-lime` off the root because MapLibre paint properties cannot read a CSS variable. The transcript became a ruled document rather than bubbles, the route card a hairline-delimited band with a figure row, and every route now carries a Sources disclosure stating that the two sources are matched by proximity and never merged.

### Phase 6 — Beta hardening
- [x] Embeddings job + semantic search behind the 503-until-populated rule. `scripts/embed_trails.py` embeds the owner-ratified input (description + landscape_description + difficulty_notes) via text-embedding-3-small and stores a sha of the input on the node, so re-runs embed only changed trails (verified live: 3 embedded, then 0 on re-run). `POST /trails/semantic-search` embeds the user's text and queries the `trail_embeddings` vector index through the named template `semantic_search_trails` — the text never becomes Cypher. Before the index is populated the endpoint returns 503, verified live against the real unpopulated index before the first embedding run. Live ranking discriminates correctly: three distinct queries each ranked their intended trail first.
- [x] Gateway pins `iss`/`aud` on Supabase tokens when `SUPABASE_URL` is set; negative tests (right key, wrong claims → 401) and a live pass with a real token. The gateway has always served `/healthz` — an earlier "missing health endpoint" finding was checked against the wrong path and is retracted.
- Wire the five UI blocks that ship inactive because the frontend has no data for them (each is a backend/API change, not a styling one):
  - **"How I read it"** — `/chat` streams results but not the plan the composer merged; return the resolved constraints so a user can correct one instead of rewriting the question.
  - **Elevation profile** — the payload carries total ascent, not a height series along the route.
  - **Places layer** — POIs do not travel with map geometry.
  - **Hazards layer** — hazards are per trail, not per segment, so there is no geometry to draw them on.
  - **Coverage layer** — nothing exposes where coverage stops, which is what the copy rules require us to name.
  - The layer tab strip that hosted three of them was removed 2026-08-27 (`frontend/components/MapChrome.tsx:5-9`); in the one-UI shell (Phase 11) layers return on the map layer's top edge when the API feeds them, and the elevation profile lives in the route panel.
- Sources disclosure shows what the payload has (graph id, OSM/ODbL, the never-merged wording). OSM way ids and the match distance render as soon as the API returns them — the component already takes them; the brand mockups' sample ids are deliberately not shipped.
- Deploy plumbing → Phase 9 (D5 closes this line; dashboards are deliberately not built — see Deploy strategy).

## Verification per phase

- Every phase lands with CI green, fully offline (`--mock`).
- Phase 1: run ingestion twice, assert node/relationship counts unchanged; cookbook queries return expected fixture results.
- Phases 2–3: end-to-end curl through gateway → backend → Neo4j on compose; auth/limit contract tests.
- Phase 4: golden set of NL queries → expected intents (50 entries incl. Italian phrasings and multiturn conversations; `--graph` checks retrieval, `--answers` checks the raw answer against the code-checkable prompt rules); adversarial set never produces a write. Scores logged per run in `backend/eval_runs.jsonl`. Phase 8 carries the eval plan.
- Phase 5: Playwright smoke — sign in, ask "easy trail near a lake", see result cards + map polyline.
- Phase 8: every full `eval_golden` run appends its line; a PR touching prompts/intents/composer/dataset carries a fresh decomposition line.
- Phase 9: `GET https://<domain>/api/healthz` 200 through Caddy after a tagged merge; one rollback and one restore rehearsed.
- Phase 10: pipeline tests pure; one regeneration per recipe over both provinces; `scripts.audit_catalogue_documents` clean; `export.review_bundle` refreshed; golden `--graph` after F3 and F5.
- Phase 11: the 360×640 Playwright project green against the live stack.

### Phase 7 — The Bergamo refactor (plan approved 2026-08-25)

A trustworthy end-to-end route system on the configured Bergamo bbox, per the
approved implementation plan (owner-ratified this session; PR #32's start/end
contract ratified with one amendment — the `:fwd` sense comes from canonical
geometry, never `edge_id`, which rebuilds reassign). Locked decisions:
geometry-derived ids for every route kind with favorites reset at cutover;
full physical schema rename via a v2 baseline; custom routes publish into the
shared catalogue after automated checks; chat scope is the query-loop plan's
Phase 2 only.

Superseded handoff next-steps (2026-08-28): "rotate the exposed key and
passwords" → Phase 9 D0; "Caddy TLS, VPS deploy script, backup cron, uptime"
→ Phase 9; "generate short hike loops near Lecco" → Phase 10 F3; "decide how
the named CAI sentieri come back" → Phase 10 F5 (decided: generate along the
relations). `/handoff` drops them from `handoff.md` on its next run.

- [x] P0 `fix/card-map-races` — a card click draws its own answer or says its
  line failed; per-card line status; payload identity verified; component
  tests + the e2e asserts drawn id == clicked id (PR #34).
- [x] P1 `fix/document-contract-checks` — the API verifies a document against
  its catalogue row (id / schema_version / build run) with typed 503s;
  `scripts.audit_catalogue_documents` swept 980/980 clean live; the loader
  stamps `doc_run_id`; the catalogue wipe stays under the transaction
  timeout (PR #35).
- [x] P2 `feat/pipeline-v2-schema` — schemas renamed to their jobs
  (source_map / catalogue / provenance) via conversion + v2 baseline;
  regions on the degree matview with the Bergamo tolerance gate measured
  (2 m holds: 0.6% at 2 m); the weld level-guard; `start_class`; `urban_m`
  (bimodal, class cuts in the valleys); oneway-honest pgRouting views; GTFS
  calendar read (trenord is NOT year-round: runs to 2026-12-12); DEM N46
  tile loaded. Ticked 2026-08-28: `sql/v2/0001_baseline.sql` (the schemas),
  `0003` (`start_class`), `0004` (`urban_m`), `0005` (oneway-honest views),
  `0006` (GTFS span) are applied — owner to confirm the weld level-guard and
  the DEM N46 tile; if either is missing, split it into its own `- [ ]` line.
- [x] P3 `feat/route-id-v2` — the id cutover (`vv2-<digest>[-fwd|-rev]` —
  hyphen, not colon: NTFS reads a colon as an Alternate Data Stream
  separator and 877 documents once vanished proving it), schema 2.0
  documents (terminals with network reachability and seasons, categories,
  divergence, continuity.reason), same-ground relations folded out loud
  (one pair: 14910465/14910466), Neo4j reloaded, favorites reset, the
  cross-layer contract fixture pinned in both suites; audit 979/979 clean.
- [x] P4 `feat/draw-out-and-back` — strict out-and-back (same edges home
  by construction, one-way 2–20 km enforced with rejects reported); legs
  run directed over the oneway-honest views; the parameter surface
  (start-class, region, exigent ceiling, ascent, retrace, urban cap);
  urban_share measured (p95 0.66, tail to 0.92) with the cap at 0.8;
  catalogue regenerated over both provinces — 1,371 routes (751 mapped +
  620 generated across six families), audit clean. The 751 mapped were
  withdrawn from the published catalogue on 2026-08-26 (see P5a). The enum CHECK
  constraints landed after a parameter-order slip corrupted a run's shapes
  and the emit drift-guard caught it downstream — the constraint now fails
  the first row at the moment of the mistake.
- [x] P5a `feat/withdraw-mapped-routes` — the catalogue publishes
  `generated` only (`export/document.py::PUBLISHED_KINDS`, read by both the
  emitter and the loader). The 751 mapped relations were the pre-factory
  corpus and measured badly — 187 with warnings, 56 under 500 m, 131 in
  pieces, 27 under 20% matched fraction — because a relation is a mapping
  of ground that our bboxes clip. `export.route_documents` withdraws by
  default and publishes only under `--publish`; the loader gates on kind
  regardless. `source_map.edge_route` untouched: the relations still name
  the network and feed `qa.v_route*`. Corpus 1,378 → 627, and the 302 named
  CAI sentieri leave the answerable set with them.
- [x] P5 `feat/compact-card` — the two-line card; exigent warning; three-
  valued bike state; surface distribution, places, real provenance and QA
  warnings on expand. Landed 2026-08-27 on PR #49
  (`frontend/components/LoopCard.tsx:157-203`, `:214-372`; ticked 2026-08-28).
- [x] P6 `feat/chat-standing-plan` — persisted standing plan (query-loop
  Phase 2); resume rehydrates cards from result_refs. Landed 2026-08-26: the
  executed plan persists as `messages.intent.standing` (same jsonb, no
  migration); `PlanEnvelope.refine` marks a delta turn and
  `composer.apply_delta` merges it latest-wins in Python; a clarify carries
  the standing forward. Resume is single-conversation (the tabs are gone):
  `GET /routes/by-ids` hydrates stored loop_ids back into cards.
  `scripts.dump_conversation` renders any conversation as markdown with
  BUG/FIX/NOTE user turns flagged — the feedback-log surface.
- ~~P7 `feat/custom-route-jobs`~~ — superseded by Phase 12 (2026-09-02): a
  custom route is drawn in-process at ask time, so there is no job to queue,
  no worker and no publication step. Quota stays the per-user LLM cost cap.
- [ ] P8 `feat/direction-documents` — the `-rev` siblings and `reverse_of`
  (additions, never renames — the id rule landed in P3).

### Phase 8 — Evals (plan drafted 2026-08-28; folds `docs/explain/eval-pipeline.html` §5)

E0–E2 and E5 are pre-launch; E3–E4 start after Phase 9 D4 go-live (the phase number is not a sequence). Doctrine unchanged (eval doc decisions ①–⑥, `:264-293`): one dataset, one checker (`backend/fixtures/golden_questions.json`, `scripts/eval_golden.py`); code checks only until error analysis names the failure modes — an unvalidated judge is worse than a named gap; pins are ids, never names; the raw stream is graded against `_answer_view`; the run log is committed. Every step is a script, never CI. Order is by cost: each step is the cheapest measurement that pays back before the next is worth buying.

Where it stands (`ece21a3`): decomposition 50/50, answers 44/44, retrieval 18/20 hit; `scripts/check_intents_live.py` 7/7 and stands alone (decision ①). Of the eval doc's five findings (`:389-408`): 1 (Neo4j lags the catalogue) is closed by P5a — 627 generated-only IS the published catalogue; 4 (stale handoff figures) is closed; 2 (the `narrowing.py` pointer) folds into E0; 3 (no short hike loops near Lecco) waits on Phase 10 F3; 5 is accepted. Gaps: `log_run` (`eval_golden.py:158-171`) writes one aggregate line, so a regression names no entry; retrieval is scored on 20 of 50 entries (19 `expect_trails` + g30's `expect_loops`); `message_feedback` (`0004`/`0005`) is written by `api/routes/feedback.py:85` and read back by nothing; the regression policy is one sentence at `README.md:282`.

| Step | Costs | Measures | Buys |
|---|---|---|---|
| E0 per-entry log | 0 OpenAI calls | which entry changed between runs | diffable lines; single-entry reruns |
| E1 retrieval coverage | one `--graph --answers` run (~59 extract + 44 answer calls) | empty-block rate; stale pins; routes in graph | retrieval read on 50 entries, not 20 |
| E2 feedback → candidates | 0 calls, `DATABASE_URL` | downvotes as golden-shaped stubs | a dataset that grows from real use |
| E3 error analysis | human hours, 0 calls | a failure-mode taxonomy with counts | the only licence to write a judge |
| E4 faithfulness judge | ~50 calls/run + hand labels | the one interpretive rule E3 ranks first | after validation only |

- [x] E0 `chore/eval-per-entry-log` (landed 2026-08-29: `failed` and `retrieval` maps on the same line, `--only` with an unknown-id guard, the `estimate_loops` comment corrected, CLAUDE.md amended; 382 backend tests green, ruff and black clean. The first USE — naming the two entries behind 18/20 — is a paid `--graph` run and is the owner's) — `log_run` (`eval_golden.py:158-171`) gains `failed: {id: [problems]}` and `retrieval: {id: first|hit|miss}` in the SAME line (one committed file, decision ⑥; the previous line is one `tail -2` away, so no diff tooling). `--only g27,g49` filters the loop at `:211` on id — four `extract_plan` calls for those two (both two-turn, `golden_questions.json:261-273`, `:479-491`; `run_turns` calls once per turn, `:144-145`) instead of ~110 — and skips `log_run`: partial totals must not enter the trend *(recommended)*. That amends "every run appends a score line" to "every full run" in `CLAUDE.md` and the docstring at `eval_golden.py:26-27`. Same branch: fold the stale pointer at `graph/queries.cypher:544` — `chat/narrowing.py` does not exist; the template is consumed at `chat/orchestrator.py:493`. First use: name the two entries behind 18/20 and mark each a data fact or an ordering bug.
- [ ] E1 `feat/eval-retrieval-coverage` — under bare `--graph`, 30 entries execute nothing because they pin no id (`eval_golden.py:225-229`); under `--graph --answers` every non-clarify plan already executes (`:227`), so these are new COUNTS over that execution, not new calls: (a) `empty_blocks: {trails: [n, executed], catalogue: [n, executed]}` — two counts, because `expect_trails` pins grade the dev fixture (`:Trail` never reaches production, D4) while `loops`/`route` are the production-relevant number. Empty is `not any(results.get(k) for k in ("loops", "trails", "route"))` — the **singular** `route` key, which is the resolved-or-None signal (`orchestrator.py:377-382`); `results["routes"]` is a non-empty list whenever a route intent ran, failed legs included (`:567`), so it cannot be the test — and not a `total_*` test, since `total_loops` is only published with a non-None total (`:368-369`) and a `loops_unknown_place` turn publishes none (`:359-360`); clarify plans excluded (clarify is the correct empty) *(recommended)*. The g27/g49 class becomes a number in the line. (b) Preflight every `expect_loops` id through `run_named("routes_by_ids", route_ids=[...])` (`queries.cypher:626-641`, `warnings = 0` at `:639`) and print an absent id as `STALE PIN`, never `MISS` — a catalogue rebuild is not a retrieval regression. (c) `routes_in_graph` = `len(run_named("catalogue_audit_index"))` (`queries.cypher:596-609`) into the line, so a score is read against the catalogue that produced it. Then pin `expect_loops` on the loop entries (a) shows non-empty (`vv2-…` ids read off the live result). Before excluding any entry, read (c) by region: the eval doc (`:278-279`) says Bergamo is not in Neo4j while the catalogue was generated and published over both provinces (Phase 7 P4) — if Bergamo loops are present, pin g20/g44 (`loop.near: Bergamo`, `golden_questions.json:194-200`, `:440-445`); if not, log "Bergamo generated routes not loaded" with the count. g27/g49 (`:262-273`, `:480-491`) carry no `expect_loops` yet: `review/routes/generated.geojson` already holds 66 Lecco loops under 18 km (the handoff's "none under 18 km" looks stale), so try pinning them now; otherwise F3 draws the 2–5 km set and E0's `--only g27,g49` re-pins. g29 (≤15 km hike + lake, `:285-297`) waits on data.
- [ ] E2 `feat/eval-feedback-candidates` — the read side of `message_feedback` (`0004_feedback.sql:1-3` names eval review as its purpose; `feedback.py:5-8` promises the join). Since U5 the key is `(user, message, route_id)` (`0006_feedback_route.sql`): `route_id = ''` is the answer-level vote, a non-empty one names the card that did not belong — negative evidence pointing at the retrieval half of a stub, which the message-level vote could never localise. It does not become an `expect_loops` entry by itself: pins stay positive and hand-written, as the rest of this line already says. `MESSAGES` (`dump_conversation.py:45-50`) left-joins `vote, comment, expected, route_id`; `render_message` (`:63-87`) prints them under the assistant turn, tested in `tests/test_dump_conversation.py`. `--downvotes` limits the dump to conversations holding `vote = -1`; `--json` emits one golden-shaped stub per downvote, `{"id": "fb-<message_id[:8]>", "turns": [every earlier user turn], "expect": {}}` — `expect` stays empty on purpose: the owner writes the expectation, the stub saves the transcription; the standing plan via `readback.describe` (`readback.py:214`), result ids, `comment` and `expected` ride along as reviewer context. Upvotes are not read: a thumbs-up on a wrong answer is the case a judge exists for. PII rule *(recommended)*: real user question text may be copied into `golden_questions.json` (`question`/`turns` only), never a user or conversation id, personal details stripped by hand; `fb-<message_id[:8]>` is the only identifier that travels, for provenance (real user vs written). Runs with the owner `DATABASE_URL` like `dump_conversation` (`:15`, `:117`) — the RLS policy (`0004:29-31`) is select-own. Done when: the first live downvote renders under its assistant turn in `dump_conversation --downvotes`, and its `--json` stub, with an owner-written `expect`, passes decomposition as an `fb-` entry.
- [ ] E3 `docs/eval-error-analysis` — deferred to launch (eval doc `:385`); "launch" = Phase 9 D4 go-live. Trigger *(recommended)*: the first of 20 downvotes, 30 non-owner conversations, or 30 days after D4, accepting a provisional taxonomy under ~50 turns; until then only the pre-launch pass over the owner's BUG/FIX/NOTE turns (`dump_conversation.py:37`) runs, expected to find data gaps rather than model failures — write that down if so. Corpus: `dump_conversation --all` + E2's stubs; draw = all downvoted turns + a uniform sample to ~100 assistant turns. Method: `evals-skills:error-analysis` — open-code first (one free-text note per turn in `backend/fixtures/eval_labels.jsonl`: `{message_id, verdict, note}`, ids only, the text stays in Supabase), then axial-code into ≤10 modes, each tagged `stage ∈ {decomposition, composition, retrieval, answer, data}`; stop at saturation (20 consecutive turns adding no mode). Output `docs/evals.md`: the taxonomy with counts and one disposition per mode — **code check** (extend `check_plan`/`check_answer`, offline-tested in `tests/test_eval_golden_checks.py`), **dataset entry** (pin it), **prompt fix** (prompt and checker in one commit, the P1 rule), **data fix** (a pipeline issue), or **judge candidate**.
- [ ] E4 `feat/eval-faithfulness-judge` — blocked on E3 by design. Scope *(recommended)*: the one rule at `chat/prompts.py:129-131` ("Use ONLY the facts in the RESULTS block. Never invent…"); all other interpretive rules (verbatim naming; empty-block phrasing; `semantic_unavailable` / `loops_unknown_place` wording, `prompts.py:130-136`) wait for their own E3 count. Binary pass/fail + one-line reason via `to_strict_schema()` (`intents.py:139`), input = raw answer + `_answer_view(results)` (decisions ④⑤). Judge model: a new `judge_model` setting beside `answer_model` (`core/config.py:69`, gpt-4o-mini), distinct from and larger than it, read only by a new `scripts/eval_judge.py` imported under `--judge` (requires `--answers`) — never by `chat/`; a test asserting `chat/` never references `judge_model` makes the convention a guard. Prompt via `evals-skills:write-judge-prompt` with E3's labelled failures as few-shots. Validation via `evals-skills:validate-evaluator` against `eval_labels.jsonl` split dev/held-out; bar: TPR ≥ 0.90 AND TNR ≥ 0.90 on held-out, written into `docs/evals.md`; below either, `--judge` does not ship and faithfulness stays in the gap list. The score line carries `judge: [pass, total]` AND the validated TPR/TNR, so a judge number never travels without its calibration; an offline test pins the verdict parsing only. Never CI; the adversarial gate is untouched.
- [ ] E5 (decision, before D4) — the `:Trail` layer in production. The only `:Trail` writer is `trailforks_ingest --mock` (`backend/ingestion/trailforks_ingest.py:33`; `osm_ingest.py` creates none) and the fixture is synthetic, so production ships with no `:Trail` nodes: `search_trails` (`chat/orchestrator.py:541`) answers nothing, `/trails/semantic-search` is 503 by rule. Decide: keep `TrailSearchIntent` (the 19 `expect_trails` entries stay a dev-fixture retrieval score, reported separately by E1) or remove it — which rewrites `golden_questions.json`, `check_intents_live`, `chat/intents.py` and the composer's `catalogue_view` fallback. *(recommended: keep the intent, ship empty; revisit when OSM-derived trails exist.)*

Regression policy *(recommended)*, written where it is read (`README.md:259-283`) and cited from the Phase 9 release checklist — one policy: a PR touching `chat/prompts.py`, `chat/intents.py`, `chat/composer.py` or the dataset carries a fresh decomposition line; **every** develop→main release PR carries `--graph --answers` (+ `--judge` once E4 lands), and `check_intents_live` 7/7 when a prompt or intent changed; the README gains the `--only` and `--downvotes`/`--json` invocations.

Deliberately not planned: `asyncio.gather` over entries (5 min at 50 entries; add past ~200); `--diff` tooling (the per-entry line and `git log` answer it); logging the adversarial gate (binary, stands alone); `generate-synthetic-data` (volume before error analysis is not signal; real downvotes are the growth path); `build-review-interface` (the markdown dump beside the labels file is the interface for one annotator; revisit at a second annotator or ~200 turns); `eval-audit` (done 2026-08-26, its findings are this phase); a CI hash guard over the prompt files (makes CI red the normal state of a WIP prompt branch and blocks anyone without `OPENAI_API_KEY`).

### Phase 9 — Deploy (ratified 2026-08-28)

- [ ] D0 (no branch) — rotate the OpenAI key and the Supabase database and account passwords (the two high-severity handoff blockers; this supersedes the handoff next-step); mint production-only `NEO4J_PASSWORD` and `GATEWAY_SHARED_SECRET`; register the domain; clear the two blockers in `handoff.md`. Nothing in D1–D5 lands on a server before this.
- [ ] D1 `feat/dockerfiles` — `backend/Dockerfile` (root context, `uv sync --frozen --no-dev`, commit `backend/uv.lock`), `gateway/Dockerfile`, `frontend/Dockerfile` + `output: 'standalone'` in `next.config.mjs`, one root `.dockerignore`; `docker-compose.yml:152-153` comment updated. *(recommended)* `Settings.vaivia_env` + boot refusal when it is `production` and `gateway_shared_secret` is empty (`core/config.py:57-60`, `api/middleware.py:67-68`), mirroring `gateway/src/config.ts:29-37`, tested like `gateway/test/config.test.ts:6-14`. Done when each image boots locally and its healthz answers.
- [ ] D2 `feat/prod-compose` — `infra/compose.prod.yml` (caddy + three app services; the `environment:` block; neo4j image pinned; body-checking healthchecks; logging caps; `/srv/vaivia/{documents,migrations}` mounts), `profiles: ["dev"]` on graphhopper/postgis and the `:1-3` header in the dev file, `infra/Caddyfile` (`handle_path /api/*`, `flush_interval -1`), `infra/deploy.sh` (preflight → migrate → pull → up → healthz + `gds.version()`), `infra/backup.sh`, `docs/deploy.md` with the once-only bootstrap (`deploy` user, ufw 22/80/443, `/srv/vaivia/{.env,documents,migrations,backups}`, first migrations from the workstation per Phase 0, GDS jar per `CONTRIBUTING.md:94`). Done when the prod compose runs on the laptop with `DOMAIN=localhost` and a streamed `/chat` answer arrives token by token through Caddy.
- [ ] D3 `feat/ci-deploy` — `images` (GHCR push, `permissions: packages: write`, packages public) and `deploy` jobs in `ci.yml`, both `if: github.ref == 'refs/heads/main'`, `deploy` `needs:` all five; secrets/vars as listed; `main`'s required status checks on GitHub updated to the four suites + `images` (`deploy` is **not** required — it never runs on a PR); VPS bootstrapped per `docs/deploy.md`. First real deploy = `v0.1.0`; rollback rehearsed once with `deploy.sh <previous sha>` on the live box. Fix `CONTRIBUTING.md:256`, `:319`, `:379` ("three" jobs — four, five with `images`) in the same PR.
- [ ] D4 `feat/publish-pack` (was `publish-catalogue`; renamed 2026-09-02 by Phase 12) — `infra/publish-pack.sh` (rsync `packs/<run_id>/` + places/starts load through the tunnel → backend reload → audit, `--dry-run`); first fill over the tunnel (schema, both regions' places and starts, embeddings, the pack); the backend's `/healthz` reports the pack `run_id`; an on-demand route's geometry served over `https://<domain>/api`. Until slice 7 of Phase 12 lands, the catalogue documents ship alongside the pack. Go-live checks in `handoff.md`: `GATEWAY_DEV_NO_AUTH` absent, hosted JWKS, `ALLOWED_ORIGINS`, Supabase Site URL / redirect URLs pointed at `<domain>` (the confirm-email flow, Phase 5's sign-in bullet), post-deploy e2e green. E5 decided before this lands.
- [ ] D5 `chore/backups-and-uptime` — `backup.sh` in cron (keep 14); heartbeat cron → healthchecks.io, e-mail verified by stopping the backend for six minutes; **one restore rehearsed**: last night's `pg_dump` into the local Supabase, `scripts.smoke_supabase` green; date recorded here. Closes Phase 6's deploy line.

### Phase 10 — Route factory v2 (drafted 2026-08-28; partly superseded 2026-09-02)

**Phase 12 supersedes this phase as catalogue generation.** F0 (recipe registry, labels, delete-by-recipe), F3 (short loops) and F4 (summit out-and-back) are not built: an on-demand planner draws the short loop or the summit route when asked, from the same rules, and there is no set to label or delete. What survives, relocated: F1's connectivity invariant and `urban_run_max_m`, and F2's `mini_loop` / `spur` / `parallel_go_and_back` rules become **planner invariants** in the shared package (Phase 12 slice 2; decision 1 in `docs/route-design.md` — they are what replaces the QGIS review for a route a user sees seconds after asking). F5 *sentiero* becomes a planner theme (a spine discount on the pack's cost columns) after slice 4. F6 urban areas and F7's `paved()` classification feed the pack (`edge_urban_m`, surface codes) and stay useful as pipeline work. The recipe and review tables below are kept as the record of the rules and tolerances; read them as planner constraints, not as a factory.

v1, in the code (`pipeline/draw/`): six families are six `draw.generate` invocations (`--shape` × `--activity`); persistence replaces per `(activity, shape)` and ignores `--region` (a Bergamo rerun deletes Lecco); a run's parameters live in `provenance.build_run` under its `run_id`, but nothing on the route row survives across runs to name the set that drew it; the only gate is `_rejected()` over `Assembled` scalars, so a mini-loop (distinct edges, retrace 0) and a go-and-back on a parallel street (different edge ids) both pass; the same-ground fold is counted only in aggregate (`kept − routes`, `generate.py:712-716`) — not per recipe, and not which row absorbed the candidate; `assemble.concatenate` emits a jumping line on a hole while `draw/emit.py` hard-codes `pieces=1`. Phase 7's P7 (custom routes) and P8 (direction documents) stay open beside this phase.

Locked (owner, 2026-08-28): five parametrised kinds; every route labelled with the recipe that built it so a bad set deletes wholesale; a review stage that rejects or modifies candidates, rules open; urban areas as polygons. No new `shape`/`kind` enum value and no schema release: `provenance.generation` is open by design (`route-document.schema.json`), `kind` stays `generated` (`export/document.py::PUBLISHED_KINDS`).

| Decision | Choice |
|---|---|
| Recipe | Frozen dataclass `draw/recipes.py::Recipe`: `name, version, activity, shape, start_classes, min_anchors, min_urban_area_m2, car_free_only, destination_kinds, named_only, distances, one_way_band_m, total_band_m, max_sac_exigent, max_ascent_m, max_retrace, max_urban_share, max_urban_run_m, review_rules`. `REGISTRY: dict[str, Recipe]`; the six legacy families are the entries `legacy-<activity>-<shape>@0` carrying today's constants (`OUT_AND_BACK_ONE_WAY_M`, `TOTAL_BAND_M`; destination no band), so the first v2 run reproduces the catalogue and replaces the backfilled rows. CLI becomes `--recipe <name>`; only run scope stays free (`--starts --seeds --keep --region --dry-run --emit`) *(recommended)*. |
| Label | `catalogue.route.recipe = '<name>@<version>'` + `parameters jsonb` (the resolved set + `builder: draw.generate/<BUILDER_VERSION>`). `tests/test_recipes.py` pins `sha256(parameters)` per label in a fixture: a parameter edit OR a builder bump without a version bump fails the build, so the label always IS parameter set + builder. |
| Replace *(recommended)* | A scoped run (`--region R`) replaces `recipe AND scope = R`; an unscoped run replaces `recipe` across every scope. `--delete-recipe <label> [--scope R] [--dry-run]` deletes the same way, counts first, writes a `build_run` row, prints the two follow-ups (emit removes what its manifest lists plus the legacy `generated-*` files; Neo4j is wiped on reload by `export.neo4j_load`). Older versions of a name stay until deleted. |
| Review stage | `draw/review.py`, pure over `list[WalkedEdge]` + `Recipe`: `Keep \| Reject(rule) \| Modify(sequence, rule, note)`. Order, extracted into a pure `finish()` at the `assemble → _rejected → candidate` seam of `generate.main`: draw → modify rules → `assert_connected` → `assemble` → `_rejected(facts, recipe)` → reject rules → candidate → `keep_distinct` → mint. The id is minted last because a modification moves the coords. |
| Surfaces *(recommended)* | Written only on a real run under the draw `run_id` (`--dry-run` returns before the `build_run` insert; `qa.finding.run_id` is a NOT NULL FK). Reject → `qa.finding` rule `review_<rule>`, `geom` = the candidate line. Modify → `qa.fix`, `target = <run_id>:<start_vertex>:<target_m>:<seed>` (no route_id yet), whole line before/after (`topology/repair.py::_fix` pattern). Views: one `qa.v_review_<rule>` per finding rule (the `qa.v_gap_dangle_*` pattern) and one `qa.v_review_fix` styled by `rule`. `qa.latest_run` is frozen topology-only, so these scope on the latest `stage = 'draw'` run inline. `provenance.generation.recipe` and `.review` in the document; no new quality warning: a rejected candidate is not emitted, a modified one is a clean route. |
| Tolerances | `--review-measure`: every rule detect-only, the distribution printed, exit, nothing written (the `topology.qa --measure` posture). The number lands in the recipe. |
| Urban | `source_map.urban_area (urban_area_id, name, area_m2, regions, geom, run_id)`: `ST_Dump` of the residential union BEFORE the subdivide `curate/urban.py` already builds; `name` = the largest named constituent polygon's, else NULL. The measure stays per edge (`edge.urban_m`, bimodal); the new route number `urban_run_max_m` — longest contiguous INTERIOR urban run (touching neither end), pure over `WalkedEdge.urban_m`, None when any is None — goes on `Assembled` with its cap in `_rejected`, a stated limit like the bands. "City" = `area_m2 >= recipe.min_urban_area_m2` (cuts measured, `size_class`) *(recommended)*; hamlets stay valid `settlement` starts. |

#### The recipes (choices marked *(recommended)* await ratification)

| recipe | activity | shape | starts | top / destination | band (stated) | caps | name |
|---|---|---|---|---|---|---|---|
| `short-loop@1` | foot | loop | `settlement`, `urban_exit`; `min_anchors 1` | — | total 2–5 km | urban run ≤ 1 km; `mini_loop` | none |
| `summit@1` | foot | out_and_back (strict, `draw_strict_out_and_back`) | today's `STARTS` | `peak`, `viewpoint`, `hut`, named only *(recommended)* | one-way 2–20 km | exigent grade per recipe | "To <place>" (`destinations.route_name`) |
| `sentiero@1` | foot only | out_and_back, strict *(recommended: `destination` dilutes the sentiero; a one-way `traverse` needs a schema minor, `route_shape_check`, `DIRECTED_SHAPES` and Phase 7 P8 first)* | a `qa.v_start` vertex on the spine, else the nearest within a measured network distance | the spine's far end | total 4–40 km | `spine_share` ≥ floor (measured) | relation `ref` + `name` (`staging.osm_relation.tags`) |
| `city-escape@1` | foot | loop | `urban_exit` of ONE `urban_area` with `area_m2 >= min_urban_area_m2` | — | total 4–15 km | urban run ≤ 1 km; `max_urban_share` 0.3 | `identity.from` = the area's name when it has one |
| `road-up-trail-down@1` | mtb | loop, two legs (+ optional paved leg home) | `place.source = 'urban_exit'` rows whose way out is a road, read regardless of `is_start` *(recommended: `anchors.urban_exit_verdict` rejects roads today)* | highest `vertex.elevation_m` in the crow band over bike edges, ranked by climb *(recommended: a climb, not a place)* | total 10–25 km, ascent ≥ 300 m *(recommended opening band)* | climb-leg paved share ≥ p, descent-leg unpaved share ≥ q, measured first | none |

`sentiero` mechanics: spine = the relation's edges in `source_map.edge_route` ordered by `(member_index, piece_index)`, longest piece of the `ST_LineMerge` (`qa.v_route.pieces`). `edges_sql()` takes `{edge_id: factor}` (today one `penalised` set × 3.0): spine edges discounted, so the router prefers the sentiero without being forced across a gap; a `-1` arc is never resurrected; the fixed-template-plus-integer-ids rule stays — a factor is a float literal, never text. `spine_share` = metres on spine edges / total. `identity.ref` is None today and `name` only for destination rows; sentiero fills both from the relation, `osm_relation_id` stays null, `rel_id` under `generation.spine` — the document is drawn over the relation, it is not the relation. Counts: relations attempted / drawn / rejected by reason.

#### The review stage — candidate rules (OPEN until built)

| rule | detects (walked sequence) | verdict | tolerance |
|---|---|---|---|
| `mini_loop` | the walk returns to a vertex visited < L m earlier | **modify**: excise `[i+1..j]`; both ends are one vertex so connectivity holds by construction | L from `--review-measure` |
| `parallel_go_and_back` | sub-walk a→b in urban fabric, crow(a,b) < d, walked ≫ crow, thin enclosed area | **detect-only** in F2; **reject** once the distribution is read; a re-routing repair is a later item | d, t |
| `spur` | palindromic sub-sequence inside a loop whose tip is not the destination and has no place within `PLACES_M` | **modify**: excise; exempt for `out_and_back` (the whole walk is the palindrome, `assemble.strict_return`) | max spur m |

Invariants for every rule:
- **Connectivity.** Consecutive walked end = next walked start. `WalkedEdge` gains `source`/`target` (`EDGE_DETAILS` already fetches them; `walked_sequence` discards them today). A violation raises before anything persists; `emit.py` derives `pieces` from the stored sequence.
- **Modify before mint.** A modification re-runs assemble and `_rejected`; rules are pure functions of (sequence, recipe), so the same ask draws the same route.
- **Every drop and fold counted.** `rejections` gains `review:<rule>`; fixes per rule; folds per recipe; all into `build_run.counts`. `--dry-run` prints, writes nothing. Score stays descriptive (`assemble.score`, `metadata-rules.md`).
- **Look before repairing.** A rule ships detect-only first; its verdict is switched on after the tolerance is read from the distribution and examples opened in QGIS.

#### Checklist

One migration per branch, numbered in landing order; each recreates `qa.v_draw` whole (DROP+CREATE) and adds its columns to `review_bundle.LAYERS` `draw` (it selects by name); each ends with `export.review_bundle` refreshed and `review/REVIEW.md` naming the round's ask.

- [ ] F0 `feat/recipe-registry` — `draw/recipes.py`; `generate.py` `--recipe`, replace per recipe/scope as above, `recipe`/`scope`/`parameters` on the row and the label in `build_run.parameters`, folds counted; band gates in `_rejected` move onto `recipe.one_way_band_m`/`total_band_m` (F3's 2–5 km is impossible while `TOTAL_BAND_M` is a constant); `--delete-recipe`; `sql/v2/0010_route_recipe.sql`: `recipe text`, `scope text`, `parameters jsonb`, idempotent backfill `legacy-<activity>-<shape>@0 WHERE recipe IS NULL`, index `(recipe, scope)`, `qa.v_draw` + `recipe`; `emit.py` `generation.recipe`; `tests/test_recipes.py` (digest per label incl. builder, activity in `EDGES_BASE`, shape in the enum, bands ordered, every backfill label is a registry key, the six legacy entries reproduce today's bands; `_rejected` gets its first test — none names it today). Docs: `pipeline/README.md` "Generating routes", `docs/route-document.md` provenance, `pipeline/docs/metadata-rules.md` "The parametric factory", `docs/fragilities.md` #18: delete-by-recipe leaves Neo4j and `review/routes/` stale until reload/emit; on the server that reload is `publish-catalogue.sh` (Phase 9 D4), whose count-mismatch guard is what protects a multi-run catalogue.
- [ ] F1 `feat/walked-topology` — `WalkedEdge.source/target` (default None; both `edge()` factories in `tests/test_draw.py` untouched); `assemble.assert_connected()`, `urban_run_max_m()` on `Assembled`, cap in `_rejected`, `legs(sequence, cut)`; `generate.py` asserts before assemble; `emit.py` derives `pieces`; `0011_urban_run.sql`: `route.urban_run_max_m` + `urban_run_class` (cuts measured). Tests as sentences: "a hole in the sequence fails the invariant", "an urban run is measured along the walk, not summed", "absent is not zero".
- [ ] F2 `feat/draw-review` — `draw/review.py`, `review(sequence, recipe) -> Reviewed`, pure `finish()`; `--review-measure`; `0012_review.sql`: `qa.v_review_mini_loop`, `qa.v_review_parallel_go_and_back`, `qa.v_review_spur`, `qa.v_review_fix`, `qa.v_draw.review_class` (`0 untouched / 1 modified / 9 not reviewed`); bundle layers + `FIELD_NOTES`; `generation.review`. Ships `mini_loop` detect-only, then modify once L is measured; `parallel_go_and_back` and `spur` detect-only. REVIEW.md ask: open the three finding layers, does each excision remove what a walker would call a detour? `tests/test_review.py`: "a mini loop is excised and the walk stays connected", "a spur to the destination is kept", "a strict out-and-back is not a spur", "a modification changes the coords before the id is minted", "every reject is counted under its rule". `metadata-rules.md` gains the review stage (seam, verdicts, invariants, rule table marked open).
- [ ] F3 `feat/short-loops` — `short-loop@1`; the `STARTS` gate `anchors >= 2 OR car_free` becomes `recipe.min_anchors`; `loops.RING_SHARE` (calibrated on 5–15 km asks) re-measured at 2–5 km; regenerate both provinces; then E0's `scripts.eval_golden --graph --only g27,g49` pins them (they carry no `expect_loops` yet, `golden_questions.json:262-273`, `:480-491`; E1 checks first whether today's 66 sub-18 km Lecco loops already answer). Supersedes the handoff "short hike loops near Lecco" next-step.
- [ ] F4 `feat/summit-out-and-back` — `summit@1`: `destination_pool()` takes `recipe.destination_kinds` (the SQL already takes `kinds`) and `named_only` as a null-idiom parameter; `--max-retrace` ignored for the shape becomes the recipe not setting it.
- [ ] F5 `feat/along-sentiero` — `SPINE` query; `edges_sql` factor map; `0013_route_spine.sql`: `route.rel_id`, `spine_share` + `spine_class`; `generation.spine`; `--review-measure` first for the discount, the floor and the start-snap distance; identity from the relation; counts. REVIEW.md ask: colour by `spine_class`, where does the router leave the sentiero and why. Success measure: how many of the 302 withdrawn names answer again (`docs/route-document.md`); golden `--graph` after. Decides and supersedes the handoff "how the named CAI sentieri come back" next-step (generate along the relations, not re-admit mapped routes). Tests: `spine_share` pure; a discount never turns a forbidden arc legal.
- [ ] F6 `feat/urban-areas` — `0014_urban_area.sql`: `source_map.urban_area` written by `curate/urban.py` from the pre-subdivide union (replaced per run, like `place`), `qa.v_urban_area` with `size_class` (cuts measured); `place.urban_area_id` on urban exits (`URBAN_EXITS` in `curate/places.py`), rewritten by `curate.places` after every `curate.urban` run or it dangles; `city-escape@1`; `identity.from`. Tests: an unnamed area gives no name; the largest named constituent names a merged area. `metadata-rules.md`: the layer and the city rule. The run cap works without polygons; polygons add identity, the start set and the layer.
- [ ] F7 `feat/bike-road-up-trail-down` — pure `paved(highway, surface)` beside `load/legality.py` (`qa.surface_class`'s groups, highway fallback for the untagged), applied in Python to one per-run fetch of `(edge_id, highway, surface)` over `routable_bike` — no view change, the ids feed the factor map; leg 1 start → top with unpaved penalised, leg 2 top → start with paved and walked penalised, optional leg 3 home; per-leg shares via `legs()`, untagged metres reported on neither side; rejects on the two floors, measured first; `0015_bike_legs.sql`: `route.climb_paved_share`, `descent_unpaved_share` + `climb_surface_class`, `descent_surface_class`. `metadata-rules.md`: the fallback table and the urban-exit-onto-road exception. Tests: both shares swap when an edge is walked backwards; the fallback never overrides a surface tag.

Not changed by this phase: the shape and kind enums (`0009`), `PUBLISHED_KINDS`, schema 2.1, the id rule (`pipeline/ids.py`), the emit drift guard, the LLM boundary.

Verification per branch: pipeline tests pure; one full regeneration per recipe over both provinces; `scripts.audit_catalogue_documents` clean; `export.review_bundle` refreshed; `neo4j_load` wipe-and-reload after any delete-by-recipe; golden eval `--graph` after F3 and F5.

### Phase 11 — One UI (layout ratified 2026-08-28)

U0 `fix/lime-everywhere` lands first. Owner-locked: one layout at every width; the conversation is the base layer and the composer never leaves the bottom edge; a tap on a route slides the map and that route's data up over the transcript; Back returns to the conversation (the Google Maps model). The sub-decisions below are chosen here with the rejected alternative named; ratify or overturn them on U1's PR.

Shaped around what must not break: `MapView` is built once and destroyed on unmount, never resized (`MapView.tsx:72-87`); `ChatPanel` is hidden, never unmounted (`page.tsx:146-168`, `ChatPanel.tsx:403`), its emissions suppressed while hidden and replayed on unhide (`ChatPanel.tsx:127-134`, `:143-149`; pinned by `test/chat-panel-wiring.test.tsx:283-304`); ODbL on every view that draws data (`BRAND-SPEC.md:267-268`), today twice (`page.tsx:187-205`, `MapView.tsx:22-23,79`); the e2e clicks a transcript card, then its `.detail-toggle` (`e2e/smoke.spec.ts:112-139`), which a layer over the transcript intercepts.

| Decision | Choice |
|---|---|
| Shell | `main.shell[data-view=chat\|map]`, a flex column: `AppHeader` (54px) / `.stage` (`flex:1; min-height:0; position:relative; display:flex; flex-direction:column; --vv-composer-h:52px` — it replaces `.chat-column`, `globals.css:61-67`, and must itself be the flex column or `.chat { flex:1; min-height:0 }` (`:69-73`) stops bounding the transcript and the composer drops below the fold) / `footer.data-credit`. `.stage` holds, in DOM order: `FavoritesView` (conditional, as today), `ChatPanel` (always mounted), `.map-layer`. `height:100vh` then `100dvh`. The `<900px` query (`globals.css:49-54`) and the `.map` column rules (`:845-850`) go; `--vv-chat-width` leaves both token copies (`tokens.css:46`, `assets/brand/tokens.css:42`); `.chat` loses its 12px margin + contour (`globals.css:69-78`) so the composer sits flush (`BRAND-SPEC.md:178`) — header and composer hairlines are the contour now. |
| Width *(recommended)* | `.shell { max-width:720px; margin:0 auto; border-left/right:1px solid var(--vv-line) }` — one rule at every width, not a second layout. Cards were cut for 400px (`globals.css:276-288`); a full-bleed 1440px transcript was rejected. Overrides `BRAND-SPEC.md:101`. |
| States | `view:'chat'\|'map'` × `showFavorites`, plus `picked: Loop\|Trail\|null`, all on the page. **chat**: transcript (or saved list) + composer + credit. **map**: `.map-layer` over the transcript or saved list, composer live under it, credit last. The composer is in every state *(recommended)*: `hidden` now hides `.messages` (`style={{display:'none'}}`, section `flex:'none'`), not the section (`ChatPanel.tsx:401-403`), so Saved routes sits above a live composer and a submit there returns to the conversation. The alternative — Saved routes as the one composer-less exception (`FavoritesView.tsx:123-165`) — was rejected: the owner said always present. |
| Always mounted | `AppHeader`, `ChatPanel`, `MapView`, `.data-credit`. Only `FavoritesView` and the panel card mount/unmount. |
| Layer geometry | `.map-layer { position:absolute; inset:0 0 var(--vv-composer-h) 0; z-index:1; display:flex; flex-direction:column; background:var(--vv-map); border-top:1px solid var(--vv-line) }`. Closed: `transform:translateY(100%); visibility:hidden` (out of tab order and hit-testing; the box keeps its size). Open: `translateY(0)`. `transition: transform 200ms, visibility 0s 200ms`; none under `prefers-reduced-motion`. Reads by the `--vv-map`/`--vv-ground` step and the hairline: no shadow, no radius, no backdrop. Inside: `.map-canvas` (flex:1: MapView + `.map-empty`) then `.route-panel` (flex:none). The variable lives on `.stage` — a common ancestor — because `.map-layer` is a sibling of `section.chat` and cannot read one declared on `.composer`; `.composer { min-height:var(--vv-composer-h) }` consumes it. 52px is the `.ask` block (19px×2 + 12.5px line + hairline, `globals.css:807-817,760-764`); U3's 16px input (24.8px line) stays under it, so the inset does not move. |
| Z-layers | 0 stage content · 1 `.map-layer` · 2 inside it: `.map-note` (`globals.css:558-569`, already z2), `.map-empty`, MapLibre controls (nav top-right, `MapView.tsx:81`; attribution row at the canvas foot). Every `.map …` MapLibre restyle (`globals.css:912-960`, and the comment at `:906-910`) is re-scoped to `.map-layer …`, or the white pill, the ctrl-group shadow and radius return. Nothing higher, ever. |
| Covered transcript | `covered` → `<div className="messages" inert={covered}>` (React 19) and `inert` on FavoritesView's section, so Tab cannot reach cards under the map while the composer stays live. |
| Opens the map | The card **body** tap only: the `onSelect` sites (`ChatPanel.tsx:484-491`, `FavoritesView.tsx:144`) and `selectTrail` (`ChatPanel.tsx:151`) call a new `onPick(picked)` prop; the page sets `picked`, `view='map'`. "See more" (`onExpand`, `ChatPanel.tsx:492-499`) keeps calling `selectLoop` — detail + line under cover — without raising the map (one gesture, one meaning). The header `Map` toggle (`Icon name="trail"`, `brand.tsx:29`; `aria-pressed`) opens the layer over whatever is drawn — the only way to see a composed A→B answer, which has geometry but no card (`ChatPanel.tsx:345-358`). Geometry emissions never open it, so the unhide replay (`ChatPanel.tsx:143-149`) cannot pop the layer. |
| Back to the conversation | `view` is derived from the URL: open sets `location.hash='#map'` (a native history entry; Next's popstate handler ignores a null-state entry), `hashchange` sets `view`. Closes: the header toggle, **Escape** (document keydown while `view==='map'`), a **composer submit** (new `onAsk`, one line at the top of `submit`, `ChatPanel.tsx:290`; the suggestion buttons at `:425,:460` route through it), opening **Saved routes** (`page.tsx:133`), **Clear chat** (`page.tsx:158-164`), sign-out (`page.tsx:65-74`) — each `history.replaceState` strips the hash (one dead entry, no `history.back()` bookkeeping); **browser/Android Back** pops the entry and `hashchange` closes. Guards inline in `page.tsx`: a null pick never changes the view; Saved routes opening or closing sets `picked=null, view='chat'`. No reducer module. Selection ≠ view: after Back the transcript card stays `aria-pressed` (`globals.css:294-296`), the line stays drawn, re-tapping raises the same map. A hairline "Conversation" block over the map's top-left was rejected: one affordance, one place; Escape and Back cover the rest, and the icon set has no arrow (`BRAND-SPEC.md:219`). **Overturned 2026-09-08 (U5)**: use showed the map opening cleanly and the way out being missed — the header zigzag is where the map is *opened*, 54px from where the reader is looking, and Escape and Back are not affordances on a phone. `.map-back`, a labelled "Back to list" button at the canvas's top-left, is the visible exit; the header toggle keeps its job (the only way to a composed A→B answer). It is words, not a glyph, so the no-arrow rule stands. U1's first commit must verify a `#map` push/pop neither reloads nor remounts `Home` under Next 15's router; fallback is `history.pushState({vv:'map'})` + `popstate`. |
| Route data | `.route-panel` renders the **same** `LoopCard` (or `TrailCard`) for `picked` from page state — `detail={routeDetail}`, `favorited`/`onToggleFavorite` from the page, no `onExpand`, no `line`; `onSelect` becomes optional (`LoopCard.tsx:18`, `TrailCard.tsx:20`) so the panel passes none. New `defaultOpen` prop (`useState(defaultOpen)`, `LoopCard.tsx:85`): figures, difficulty, surface, places, profile, Sources (`LoopCard.tsx:214-372`) open on arrival — the tap asked for the data. Panel: `max-height:45%; overflow-y:auto; border-top:1px solid var(--vv-line); background:var(--vv-ground)`; on a 640px viewport the canvas keeps ~270px. Fixed height, scrolls, not draggable. The panel card is asserted as `.route-panel .route-card[data-route-id]` (`LoopCard.tsx:117`); `TrailCard` has no `data-route-id` (`TrailCard.tsx:35-39`) and the smoke never asks for trails (`smoke.spec.ts:93-96`). |
| Line status | `lineStatus` stays in `ChatPanel` (`:105`) / `FavoritesView` (`:52`); nothing new travels with the pick. `.map-empty` is the page's, gated on `picked` and `focusedRouteId(geometry)` (`mapTurn.ts:191-197`): layer up, no pick → "Nothing drawn yet — pick a route."; a pick whose id is not the drawn one → "No map line for this route." For that gate to hold for trails, `selectTrail` must stamp `id: trail.id` into the feature properties (`ChatPanel.tsx:173-177`, as `FavoritesView.tsx:109` does for loops) — the backend's trail GeoJSON carries `trail_id` only (`backend/api/routes/trails.py:113-118`), so today `focusedRouteId` is null for a correctly drawn trail. The transcript card underneath keeps the exact status and the retry (`LoopCard.tsx:146-155`). |
| Detail state | `routeDetail` becomes `RouteDetail\|null\|undefined`; `onPick` resets it to `undefined` before `routes.load` (`ChatPanel.tsx:200`, `useRouteDetails.ts:66-86`), so the panel shows "Fetching the altitude profile…" (`LoopCard.tsx:361-362`), never the previous route's profile or a false "No altitude profile" (`:363-366`). |
| Elevation profile | Only in `LoopDetail` (`LoopCard.tsx:352-360`, bars from `ElevationProfile`, `MapChrome.tsx:27-50`, which stays). `ElevationPanel` (`MapChrome.tsx:57-87`), `.map-panel` (`globals.css:861-866`) and its wiring (`page.tsx:10,15,180-183`) are deleted. `lib/profile.ts` stays: `LoopCard.tsx:8` imports it. |
| ODbL *(recommended)* | Every state: `footer.data-credit` is the shell's last row under the composer (moved from the map column, `page.tsx:187-205`; CSS `globals.css:829-833` unchanged), shortened to one sentence — "© OpenStreetMap contributors · ODbL — check conditions locally" (two lines under 400px, accepted). Map state additionally: MapLibre's full-width attribution row at the canvas foot (`MapView.tsx:79`), which the panel sits **below**, never over; `attributionControl:false` is the rejected one-line alternative. Nothing behind a toggle or a modal. |
| Map sizing | The layer is never `display:none`, so open/close does not resize the container and a `fitBounds` computed while closed is right. The panel mounting DOES shrink `.map-canvas` in the same commit as the geometry, so `MapView` calls `instance.resize()` immediately before `fitBounds` (`MapView.tsx:157-158`). Everything else is maplibre-gl 4.7's own `trackResize` (`package.json:17`; not disabled, `MapView.tsx:74-80`) — no second observer. |
| Composer | Stays inside `ChatPanel` (`ChatPanel.tsx:571-602`), visible in every state. `padding-bottom: env(safe-area-inset-bottom)`. Input 16px under `(pointer: coarse)` *(recommended)* — iOS zooms into inputs under 16px; the spec's 13.5px (`globals.css:773`, `BRAND-SPEC.md:171`) stays for `pointer: fine`; amend the spec. Placeholder stays "Type what you want to do" (`:581`): no mic ships. |
| Keyboard | `layout.tsx` gains `export const viewport = { width:'device-width', initialScale:1, viewportFit:'cover', interactiveWidget:'resizes-content' }` (`layout.tsx:1-19` has none) so Chrome/Android shrinks the layout viewport and `100dvh` follows. iOS ignores it: if the device check fails, a `visualViewport` `resize` listener in `page.tsx` writes `--vv-vh` and `.shell { height: var(--vv-vh, 100dvh) }` (~6 lines). |
| Hidden guard | Unchanged in meaning: `hidden` ⇔ the Saved-routes view owns the transcript and the map (`ChatPanel.tsx:65-71,123-134`). `onPick` joins `emitGeometry`/`emitDetail` behind `hiddenRef`. |
| Streaming while the map is up | A submit closes the map first (`onAsk`), so the count-first answer, a clarify and its suggestions are read on the base layer (the header tap reopens it over the new lines). A stream already running when a card was tapped continues underneath (tokens land at `:374-377`); its `results` reset (`:338-340`) also calls `onPick(null)`: the panel card clears, the new answer's unfocused lines draw, the layer stays where it was. |
| Resets | The `[user]` effect (`page.tsx:70-73`) and `onClear` (`:158-164`) also set `picked=null, view='chat'` and strip the hash: `Home` stays mounted across sign-out, so the next account must not inherit an open map. |
| Header at 360px | Mark + wordmark + spacer + Saved + Map + Sign out ≈ 285px by the token metrics (`globals.css:83-148`); the 84px email (`globals.css:110-115`, `AppHeader.tsx:42-46`) leaves and becomes `title` on Sign out. The e2e's signed-in signal changes from `getByText(EMAIL)` (`smoke.spec.ts:50,64,90`) to the Sign out button. |
| URL state | `#map` only. Deep links deferred to U4 — `fetchRoutesByIds` exists (`api.ts:167-174`, `routing.py:283-300`), but the share affordance that would write the URL does not (`docs/social-layer.md`). |
| Dependencies | None. `inert`, `hashchange`, `visualViewport` are platform; Playwright `devices` is installed (`package.json:23`). |

State table:

| State | transcript / saved list | `.map-layer` | `.route-panel` | composer | credit |
|---|---|---|---|---|---|
| chat | visible, scrollable | translated off, `visibility:hidden` | — | live | last row |
| map (loop) | under the layer, `inert`, scroll kept | up; nav top-right; attribution row at the canvas foot | one `LoopCard`, open, ≤45%, scrolls | live; submit closes the map | last row |
| map (trail / composed) | as above | up | `TrailCard` / none | live | last row |
| Saved routes (+ either) | the saved list (`.messages` of the chat hidden) | as per view | as per view | live; submit closes Saved routes and the map | last row |

Tests. vitest: `chat-panel-wiring.test.tsx` — body tap calls `onPick` with its loop, "See more" does not, submit calls `onAsk`, a results reset mid-map calls `onPick(null)`, hidden hides `.messages` and not the form, the hidden-replay case (`:283-304`) untouched; `favorites-view.test.tsx`: `onPick` on select; `chat-panel.test.tsx` (jsdom) renders `LoopCard` directly: `defaultOpen` shows `.route-detail` without a click. MapView stays un-rendered (no maplibre mock) — `resize()` is covered by e2e only. Playwright: `playwright.config.ts` `use` (`:19-22`) becomes `viewport 360×640, isMobile, hasTouch, deviceScaleFactor 3` — one project, since there is one UI. Smoke assertions that change: `:50,:64,:90` signed-in = Sign out button visible; `:112-118` after the click: `.map-layer` visible, `.route-panel .route-card` has `data-route-id == clickedId`, `[data-selected-route]` as today, `.map-empty` hidden; `:135-139` the panel card is open by default, so `.route-panel .route-detail .profile i` is visible directly; then `Escape` → `.map-layer` hidden, transcript card still `aria-pressed`; re-tap → `page.goBack()` → hidden; the transcript card's own `.detail-toggle` → `.messages .route-detail .profile i`; `:146-153` show-more and `:158-168` feedback run with the map closed; `:172-179` the saved card click opens the layer, `Escape`, then unsave. New: `.data-credit` visible in both states, `.maplibregl-ctrl-attrib` visible in map state, header `scrollWidth <= clientWidth`, `.maplibregl-canvas` width == `.map-canvas` width after open. Every locator scopes to `.messages`, `.favorites-view` or `.route-panel`: the panel card shares `.route-card`/`data-route-id`, so an unscoped `.first()` re-resolves after the click.

- [x] U0 `fix/lime-everywhere` — lime for every line, selection by width/opacity only. Landed 2026-08-28; 106 frontend tests green, `next build` clean:
  - `frontend/components/MapView.tsx:120-140`: replace the `bandColor` `match` with a single `token('--vv-lime', '#CCFF3B')`; keep `line-width` `['case', selected, 4, 2]` and `line-opacity` `['case', selected, 1, 0.4]` (`:150-151`) — that is the "soft" the owner means. Delete the band/mtb comments.
  - `frontend/lib/mapTurn.ts:142-162`: drop the `bands` parameter and the `difficulty_band` property; remove the `bands` ref (`frontend/components/ChatPanel.tsx:102`) and its three writers (`ChatPanel.tsx:176,249`, `frontend/components/FavoritesView.tsx:111`).
  - `frontend/lib/difficulty.ts`: delete `DifficultyBand`, `bandOfRank`, `loopBand`, `trailBand` (only the call sites above and the test import them); keep `SAC_ORDER` — `LoopCard.tsx:5,48` uses it. Delete `frontend/test/difficulty.test.ts` (band tests only) and the `difficulty_band` assertions in `frontend/test/line-status.test.ts:133-148`.
  - Tokens: `--vv-amber` is used only by `MapView.tsx:130` and the spec — delete it from `frontend/app/tokens.css:29` and `assets/brand/tokens.css:25` (byte-identical apart from the header).
  - `assets/brand/BRAND-SPEC.md` (same PR): strike the 2026-08-27 amendment (`:56-61`), the `--vv-amber` row (`:38`) and the mtb note on `--vv-ground` (`:29-30`); add "Amendment 2026-08-28: every route line is `--vv-lime`; selection is encoded by width and opacity (4 px / 1.0 vs 2 px / 0.4), never by colour. Two accents again." The §6 card-square rule (`:216-218`) is unbuilt and untouched.
  - Verify: `cd frontend && npm test && npm run build`; e2e `smoke.spec.ts:112-118` unchanged (it asserts selection, not colour).
- [x] U1 `feat/one-ui-shell` (landed 2026-08-29 together with U2 — the panel and the shell share `picked`/`routeDetail`, so splitting them would have written the empty-state gating twice; 116 frontend tests green, `next build` and `tsc` clean, the shell driven in a real browser at 360x640 and 1280x800 — card-to-panel is covered by unit tests only, the gateway was down; e2e updated but unrun) — `page.tsx`: the column shell, `.stage`, `.map-layer` (MapView + `.map-empty` + an empty `.route-panel` slot), credit as last row with the one-sentence copy, `picked`/`view` state with the hash listener, Escape, the two guards and the resets, `ElevationPanel` removed (`profileFromDetail` import goes with it; the module stays); `globals.css`: `.shell` rows + `100dvh` + the 720px cap, media query and `.map` rules (`:845-850`) replaced by `.stage/.map-layer/.route-panel`, `.map …` MapLibre rules (`:906-960`) re-scoped to `.map-layer …`, `.chat` contour dropped, `--vv-composer-h` on `.stage` consumed by `.composer`, `.map-panel` deleted; `tokens.css` ×2: `--vv-chat-width` removed; `AppHeader.tsx`: `Map` toggle (`aria-pressed`, `aria-label="Map"`), email → Sign out `title`; `ChatPanel` `onPick`/`onAsk`/`covered`, `hidden` moved onto `.messages` (a `pick()` helper folds the three `routes.select(null); setSelectedTrail(null)` pairs at `:239,:338,:367`); `FavoritesView` `onPick` + `inert`; `MapView.tsx` `resize()` before `fitBounds`; `.map-empty` copy. Docs in the same PR: BRAND-SPEC §4 `:100-101` (one column, 720px cap) and §5 `:181-188` (profile in the panel, attribution row kept). Tests: the wiring cases, `playwright.config.ts` viewport, the smoke edits above minus the panel-card ones.
- [x] U2 `feat/route-panel` (landed with U1) — `LoopCard` `defaultOpen` + optional `onSelect` (both cards); the panel renders `LoopCard`/`TrailCard` for `picked`; `routeDetail` tri-state (`undefined` on pick); panel scroll rules; smoke `:112-139` rewritten to the panel card; the `chat-panel.test.tsx` `defaultOpen` case; the mid-map results-reset wiring test.
- [x] U3 `feat/composer-viewport` (landed 2026-08-29; the `--vv-vh` visualViewport fallback deliberately NOT written — it cannot be tested without an iOS device, and the manual checklist in `e2e/smoke.spec.ts` says when it becomes due. Also: `.stage` needs `overflow: clip`, not `hidden` — found in the browser on U1: the closed layer's `translateY(100%)` counts as scrollable overflow, and `hidden` still makes a scroll container, so `scrollIntoView` dragged the whole conversation up and the header off the screen) — `layout.tsx` `viewport` export; safe-area padding; 16px input under `(pointer: coarse)` + the spec amendment at `BRAND-SPEC.md:171`; a manual device checklist in the `smoke.spec.ts` header comment (iOS Safari, Chrome Android: keyboard up → composer visible, canvas not stretched), since Playwright cannot raise a soft keyboard; the `--vv-vh` `visualViewport` listener lands only if the iOS check fails.
- [ ] U4 `feat/route-deep-link` — note first (measured in the browser 2026-09-08): **no hash survives load today**, `#map` included — the `[user]` effect calls `closeMap()` on mount, which `replaceState`s the hash away before anything can read it, so U4 must read the hash before that effect runs, not after. `#r=<id>` on load → `fetchRoutesByIds` → `onPick`; missing id → the panel's not-in-catalogue line; written on open so a copied URL works. Deferred until the social layer's share affordance (`docs/social-layer.md`).
- [ ] U5 `feat/route-feedback` (built 2026-09-08, in the working tree, not yet a PR) — two things from one session of real use. **The exit**: `.map-back` ("Back to list") absolute at `.map-canvas`'s top-left, z2 beside `.map-empty` and the nav control, `onClick={closeMap}`; the decision it overturns is recorded above. E2E: the smoke's Escape block gains a `.map-back` click. **The thumbs on a route**: `Feedback.tsx` takes an optional `routeId` — same two-field downvote form, same copy, the question reads "Was this route right?" and the thumbs are `Good/Bad route` so a card's ask and its answer's ask are distinguishable to a screen reader. `LoopCard` renders it at the foot of `.route-detail` (so: on expand) when given `messageId`+`conversationId`, wrapped in a `stopPropagation` div — the card body is itself a button. Same fix's other half: `activateByKey` now ignores descendants' keys (`event.target !== event.currentTarget`), which was already swallowing Enter on "See more" and would have eaten every space typed into the comment. The transcript card gets both ids from `ChatPanel`; the map panel card gets them from a new `FeedbackTurn` riding `onPick(picked, turn?)` (`lib/types.ts`) — the saved-routes view passes none, because a saved route answers no question. `TrailCard` gets nothing: `:Trail` never reaches production (D4). Storage: `0006_feedback_route.sql` adds `route_id text not null default ''` and widens the PK to `(user_id, message_id, route_id)` under a `pg_constraint` guard; `feedback.py` carries it through the protocol, both stores and the `ON CONFLICT`, bounded at 128 chars at the boundary. No FK on `route_id` (catalogue ids rebuild — the `route_favorites` rule). The gateway is a prefix proxy and is untouched. The panel card is keyed by route id, so the `Feedback` inside it is keyed on `messageId` — the same route offered by two answers reuses the instance, and a carried-over vote would show as cast on a turn nobody judged and then post that turn's comment. Known ceiling: a card tapped while the answer is still streaming is picked before `done` assigns a message id, so the panel card shows no thumbs until it is tapped again — re-emitting the pick on `done` would reopen the map and refetch the detail, which is worse. Tests: `test_api_feedback.py` — two routes judged separately under one message, the answer-level row untouched, an over-long id refused; `loop-card.test.tsx` — no thumbs without a turn, the vote carries the route id, a changed turn resets it, and neither the click nor a typed space reaches `onSelect`; `smoke.spec.ts` — the `.feedback` locator is scoped to `.messages` (the lowered panel card holds one too) and the Escape block gains the `.map-back` click. Reading the rows back is E2, not this.

Explicitly deferred: a draggable/snapping sheet (the panel is fixed-height and scrolls); tapping a soft sibling line on the map to switch selection (Back → tap the card); a 1440px e2e pass over the width cap; the mic button; a conversation list; the five inactive blocks (the Phase 6 list) — the layer tabs return on the map layer's top edge when the API feeds them.

### Phase 12 — On-demand routes (ratified 2026-09-02; design in `docs/route-design.md`)

The second product feature: a route drawn for one person from what they said, at the moment they ask, over the cleaned network. **The network is the catalogue.** The pipeline stops at the network, its places and its starts, and exports them as one binary pack (`packs/<run_id>/network.npz` + manifest); the backend mmaps it and draws in-process with bounded scipy Dijkstra, using the same assembly rules, the same `ids.route_id` mint, the same schema and the same card as the 627. Measured basis (2026-09-01): GDS has no cost cutoff, so the bounded distance field every loop and out-and-back needs is 6.5 s streamed or 1–4 s pre-projected in Neo4j, against tens of milliseconds in scipy over a 35 MB pack; A→B in GDS is 23 ms and fine, but two geometric truths are one too many. Neo4j keeps meaning (places, names, favourites, map-back) and gives up geometry. The LLM boundary does not move: `OutingIntent` (activity, shape, party, fitness, hours, waypoints with roles, surface exclusions, setting, theme, start spec, sleep, area, `refine_from` ordinal) carries no query, template, id, coordinate or weight; `chat/compile.py` owns every number the planner sees; `here` is a typed `near` on `ChatRequest` that never reaches `extract_plan`. Infeasibility is a count ("42 loops in reach, all over 10 % asphalt"), the Clarify is built from the counts, chips send a typed delta straight to the composer with no model call.

Ratified (owner, 2026-09-02): ① no human review before a user sees a route — planner invariants (Phase 10 F1/F2 rules as pure functions), a 627-id parity test, a 3 × 3 km fixture pack, "report this route" on the card; ② cards show a duration labelled *our estimate* from `core/durations.py`, the schema still refuses the field; ③ drive time is pgRouting over OSM car roads in the existing PostGIS, at export time; ④ GDS retired — A→B on the pack too; ⑤ a shared package `shared/routes` (`vaivia_routes`: assemble, loops, destinations, document, ids) as an editable path dependency of both uv projects, tests in the pipeline CI job; ⑥ Neo4j heap max 1 g, tx-log retention 2 days, now.

Supersedes: Phase 7 P7; Phase 10 F0/F3/F4 as catalogue generation; Phase 9 D4 `publish-catalogue` → `publish-pack`; `docs/routing-engine.md`'s GraphHopper choice; `core/comfort.py` runtime plumbing (calibration moves into the pack's cost columns). Dies as product, stays as the parity oracle: `catalogue.route`/`route_edge`, the Neo4j catalogue load, `search_loops`/`estimate_loops`/`loop_candidates`/`loop_poi_conjunction`, `LoopSearchIntent`, `catalogue_view`, `draw/generate.py`. Golden `expect_loops` ids become `expect_facts` bands pinned to a pack `run_id`. Alternatives (leg graph, Neo4j field-and-stitch, sampler, mosaico, agentic tool loop) and the measurement table are in the design doc; the leg graph is the named fallback past ~500k edges.

- [x] R1 `feat/pack-export` (2 d; committed 2026-09-02, `a744f59`, PR pending) — `pipeline/export/pack.py`: vertices (lon/lat/component), edges (u/v, length/ascent/descent, four directional cost columns with −1 forbidden from `catalogue.v_edges_foot/bike`, surface/highway codes, sac/mtb rank, `urban_m`, `edge_id`), ragged geometry f64 + elevation f16, places (`qa.v_place`, outdoor and cultural kinds), starts (`qa.v_start`, class bitmask, car_free, anchors, seasons); `manifest.json` with `run_id`, code tables, licence block, array shapes; a fixture pack cut from a 3 × 3 km tile under `pipeline/tests/fixtures/`; round-trip test (write → load → every array shape and dtype matches the manifest, every edge's endpoints index a vertex, −1 never appears on a legal arc). Decision ⑥ in `infra/docker-compose.yml` on the same branch. *Landed as:* the format (`SPEC`, `validate`, `write`, `load`) is already the shared package `shared/routes/vaivia_routes/pack.py`, so R2 extends it rather than creating it; elevation is f32 per geometry point (`profile_m` is one sample per point); places are every `source_map.place` row with starts as the `is_start` mask and `start_class` coded — the bitmask/car_free/anchors/seasons columns wait for a planner that reads them; the fixture pack is **not cut yet** (needs live PostGIS, goes under `shared/routes/tests/fixtures/pack-lecco-3km/`). Whole store: 80,113 vertices, 101,951 edges, 761,048 points, 17,697 places, 10 s.
- [ ] R2 `feat/pack-engine` (3 d) — `shared/routes/` (`vaivia_routes`): `assemble`, `loops`, `destinations`, `document`, `ids` moved from `pipeline/draw` and `pipeline/ids.py` (re-exports left behind until R7), `[tool.uv.sources]` in both projects; `network.py` (CSR per activity, `scipy.sparse.csgraph.dijkstra(limit=)`, predecessor walk → `WalkedEdge` sequence); planner invariants (`assert_connected`, `mini_loop`, `spur`, retrace share, urban share — F1/F2 as pure functions over the walked sequence); **parity test**: over the full pack, the engine reproduces all 627 catalogue ids from `catalogue.route`'s start/target/seed (the oracle; a miss is a bug in the engine, never a reason to re-mint); latency measured on the VPS and written into the design doc (turn budget, ProcessPool size).
- [ ] R3 `feat/outing-intent` (2 d) — `OutingIntent`, `Waypoint`, `StartSpec` in `chat/intents.py` (`to_strict_schema()`, no oneOf); `LoopSearchIntent` kept until R4 wires the planner; `chat/compile.py` pure and unit-tested (pace table in `core/durations.py`, party → sac/mtb caps, setting → urban cap/floor, exclusions → cost ×4 + 10 % share cap, roles → kind sets, `area` → gazetteer or a Python Clarify naming what is covered); `near` on `ChatRequest` validated to the coverage bbox with a test that the extraction mock never sees it; golden entries for A–E plus five adversarial; `check_intents_live` 7/7; new POI kinds `church`, `museum`, `monument`, `agriturismo`, `river_access` + lodging kinds in `curate/places.py`.
- [ ] R4 `feat/outing-planner` (3 d) — backend loads the pack at startup (`Settings.pack_dir`, `/healthz` reports `run_id`; refuses to boot in production without one); `chat/planner.py` runs compile → candidates → draw → facts → reject-by-stated-limits (counted) → one relaxation rung → `keep_distinct` → 3; documents built in-process, ids minted last, cached per conversation as ordinals; `/chat` routes `OutingIntent` here and `LoopSearchIntent` to the catalogue until R7; the answer prompt receives facts + the assumptions strip + the counts, never geometry; favourite/share writes the document and one `(:Route)`; frontend: assumptions strip, chips (typed delta, no model call), "report this route". Demo: A, B, C answered end to end on the local stack. Golden: `expect_facts` bands (distance, ascent, surface share, ends-at kind) pinned to the pack `run_id`.
- [ ] R5 `feat/pack-drive-rail` (1.5 d) — `curate/drive.py`: settlement × start minutes by pgRouting over OSM car roads (one `pgr_dijkstraCost` many-to-many per build, not per ask); GTFS station × station matrix; gazetteer (named areas → polygon, `covered`); `trail_share_5km` per start; potential fields per place kind (network distance to the nearest, f16, one bounded multi-source Dijkstra per kind). Until this lands, "an hour from here" is crow-fly with the substitution said out loud and "end at a lake" is a search.
- [ ] R6 `feat/multi-day` (2 d) — day-leg chaining over sleep-kind places (`hut`, `campsite`, `agriturismo`), fitness → per-day band, `trek.json` (a route document per day + an envelope; schema minor if the envelope needs one), day cards; demo D. E stays a Clarify: Tuscany is `covered: false`.
- [ ] R7 `chore/retire-catalogue` (1 d) — drop `search_loops`/`estimate_loops`/`routes_by_ids`-for-catalogue/GDS/raw-graph templates and their guard tests, `LoopSearchIntent`, `catalogue_view`, the Neo4j catalogue and `Intersection`/`CONNECTS_TO` loads, `draw/generate.py`'s CLI (the parity oracle keeps the 627 ids and their inputs as a fixture); `publish-pack.sh`; GDS plugin off and the compose comment rewritten; `docs/architecture.md`, `docs/fragilities.md` (#18: a pack that lags the places in Neo4j maps a place to a vertex that moved — the manifest `run_id` must match the places load), `docs/routing-engine.md` marked superseded, `CLAUDE.md` graph-model rules trimmed to what remains.

Verification per slice: pipeline and shared-package tests pure (the fixture pack is the only network in CI); R2's parity test against the full pack runs on the workstation, its result recorded on the PR; backend tests green with the fixture pack; golden eval `--graph` after R4 and R5; `check_intents_live` 7/7 after R3.
