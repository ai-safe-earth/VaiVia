"""The route engine both tiers run (docs/route-design.md).

The pack format, assembly, loops, destinations, the document builder, the id
mint and the drawing functions live here, so that a route drawn by the pipeline
(the parity test) and one drawn at ask time are the same code.
"""
