# assets/brand

VaiVia brand system v1.0 — acid direction, dark ground.

    PASTE-INTO-CLAUDE-CODE.md   the instruction to paste into Claude Code
    BRAND-SPEC.md               the full spec — source of truth
    tokens.css                  CSS variables and text/component roles
    logo/                       mark, app icon, wordmark
    icons/                      10 UI icons, 20x20, 1.75px stroke, currentColor

Start with PASTE-INTO-CLAUDE-CODE.md. Everything else is referenced from there.

The mark is two nodes and one edge — the data model: two sources, matched by
proximity, never merged. Keep that meaning intact.